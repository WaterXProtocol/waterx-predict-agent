# Implementation backlog

Derived from `docs/AGENT_INSTALLATION_AND_RUNTIME_PLAN.md` and constrained by
`docs/adr/`. This file is the only place that tracks what is **actually
implemented**. The plan describes the intended system; an ADR constrains how it
gets built; neither is evidence that anything works.

- Verified: 2026-08-26
- SDK: `codex/waterx-predict-agent-runtime` @ `a9ce6f4` plus this commit
- Backend: `codex/waterx-predict-agent-runtime` @ `f14d2a5f` — unchanged by the
  release-readiness work, and **correctly so**: provenance, an SBOM and a support
  policy are properties of what this repository publishes. No wire surface is
  touched and no endpoint is added. The backend worktree is clean at that commit
  and the vendored contract was re-diffed against it below. See the Contract sync
  section.
- SDK verification: `pnpm typecheck` clean, `pnpm test` **1258 in 65 files**
  (206 SDK, 74 schema, 219 CLI, 423 Runner, 13 browser signer, 14 keystore signer, 50 adapters, 30 MCP, 95 E2E harness,
  76 release, 56 workspace), `pnpm build` clean, and all **three** generators
  reproduce their committed artifacts byte-for-byte — now **six** files, because
  each document is emitted both at the repository root and inside the package
  that ships it: `pnpm schema:generate` for `schemas/v1/agent-commands.json` and
  `packages/schema/agent-commands.json`, `pnpm instructions:generate` for
  `agent-instructions/AGENT_INSTRUCTIONS.md`, `packages/sdk/AGENT_INSTRUCTIONS.md`,
  `agent-instructions/SKILL.md` and `packages/sdk/SKILL.md`, and
  `pnpm sbom:generate` for `sbom/v1/*.cdx.json`. The Runner suite runs against real SQLite files in a temporary
  directory that are closed and reopened; it opens no socket and spawns no
  process. The adapter suites are the same: `spawn` and the `CoreInvoker` are
  injected throughout, so nothing there starts the command core, binds a port or
  reads a real stdin. The release suite reads real files in a temporary tree and
  the repository's own `node_modules`; it installs nothing and reaches no network.
  The counts above are measured, not carried forward: the previous header claimed
  1129 in 59 files, which the onboarding commit (`a9ce6f4`) had already made
  stale without restating.
- Package install was verified from the packed tarballs, not from the workspace:
  `npm pack` for `@waterx/predict-agent-sdk` (39 files) and
  `@waterx/predict-agent-schema` (18 files) carries `dist/`, `LICENSE`,
  `README.md`, `package.json` and the agent-facing documents — the SDK's
  `AGENT_INSTRUCTIONS.md` and `SKILL.md`, the schema's `agent-commands.json` —
  and nothing else: no sources, no tests, no `tsconfig`. Both tarballs were
  installed into a scratch project on **Node 22.13.1 and 26.3.1** (macOS/arm64),
  reaching the client, the exported contract constants, the schema document,
  `describeInstallation()` and both shipped documents read from
  `node_modules` by the path the report itself gives. `npx` ran the SDK's
  discovery binary from that install and exited 3 with four requirements
  missing, which is the correct answer for a project that has configured
  nothing. **Node 20 was not re-run this time** — it was verified at the
  2026-08-18 entry and nothing since has moved the floor — and **Linux and
  Windows are unverified here**; the `floor` job in
  `.github/workflows/verify.yml` covers Node 20 on Linux, and no Windows runner
  exists.
- **No end-to-end run against a live server has happened.** Nothing on this
  machine is provisioned — no `WATERX_PREDICT_*` variable, no config file, and
  no Runner listening on a socket — so the E2E harness (1.11) reports PARTIAL,
  exit 10, with one step passed and **nineteen** NOT RUN, and all ten
  provisioning gaps outstanding. Those 90 harness tests are tests **of the
  harness**; they are not evidence that the runtime works against a server.

The repository is a pnpm workspace. `packages/sdk` is the SDK this file tracked
before the split; `packages/cli` now holds discovery, `doctor`, the market and
account read plane, **and the market-order write plane** — preview, execute,
execute-many, get and reconcile, behind an enforced execution policy;
`packages/runner` now holds the job store, the state machine, lease fencing, crash
recovery **and a daemon** — a process that recovers at start-up, supervises the
leases it holds and answers an authenticated local socket — and `packages/adapters`
and `packages/mcp` now put that same command set in front of a model host, by
spawning the CLI rather than by importing anything. `packages/e2e` is a test
harness that drives the installed CLI; it ships to nobody, and a green harness
suite is not a green end-to-end. `packages/release` is the same kind of thing
from the other end: it inventories what a published package carries into a
consumer and refuses to release a workspace that is not fit to publish. Both are
`private` permanently rather than pending a release.

What the CLI writes, it writes one order at a time from a one-shot process. There
is a daemon now, and there is now also a **job driver** — `driveJob` advances one
claimed job by one pass, from watching through the fresh trigger-time checks to
independent multi-leg create/sign/submit and reconcile, and its exactly-one-
submission property is tested against a real database killed at each side-effect
boundary — and there is now a **loop**: `JobScheduler` claims runnable jobs and
calls `driveJob` on a tick, never overlapping itself, dropping a job the instant
this instance is fenced out of it, and carrying on past a job whose pass threw.

Both of the things the loop must be handed now exist as well.
`QuoteStreamPriceObserver` answers a `PriceObserver` from the SDK's indicative
quote stream and, crucially, answers *nothing* rather than a remembered number
whenever that feed cannot prove it is live. `createExternalCommandSigner` is the
signer inside the trust boundary: it signs sponsored bytes through a keystore
command that holds the key, only for a job admitted under a `delegated-auto`
policy, and refuses `interactive` and `read-only` **before** it spawns anything.

And the shipped `runnerd` can now build them. `resolveRunnerConfig` reads a base
URL, an agent wallet and a keystore argv from the environment and one JSON file,
and `buildRunnerDriver` turns that into all three collaborators over one session
and one socket — so a configured `runnerd` prints `driving: true`, which is the
test this file set for itself. It is all-or-nothing on purpose: any missing piece
yields *no* driver and names the gap, because a Runner with a gateway and no
signer would create an order it cannot authorize. Whether a daemon drives remains
a decision rather than a fact about the build, and the daemon still reports which
it is (`driving`, `driverGaps`, and now a per-topic `prices` block so a dead feed
does not read as a quiet market).

A strategy now reaches that process from outside it. `strategy.create`, `get`,
`list`, `cancel` and `events` are served on the IPC socket by the same
`StrategyService` an embedder holds, so there is one implementation of sizing,
expiry and cancellation rather than one per surface, and the mandate a job is
admitted under comes from this host's `policy` configuration rather than from the
request — which carries no policy field, because a peer that could name its own
mode would be granting itself the authority to sign unattended. The default is
`interactive`, which refuses.

**And there is now a command.** `waterx-predict strategy create / get / list /
cancel / events` speaks that protocol: five entries in the command contract, five
handlers in the CLI, and its own client for the socket. The CLI does **not**
depend on `packages/runner` to do it — that workspace edge is still refused, so
`describe`, the command you run when nothing works, does not drag a daemon, a
SQLite store and a signer behind it. The wire is the shared thing instead, the way
`SIGNER_PROTOCOL` already is: `RUNNER_IPC_PROTOCOL` is copied, `tests/workspace.test.ts`
holds the two descriptors equal, and it feeds the frames the CLI actually writes
through the Runner's real decoder. The CLI decides nothing about a strategy —
expiry, sizing, the position read and the mandate are all the Runner's, and a
symbolic refusal (`EXPIRY_REQUIRED`, `SIZE_AMBIGUOUS`) is carried out unchanged
rather than re-derived.

What has *not* changed: **nothing here has been run against a real server**, so
the "and see it fill" half of this file's own bar for 2.8 is still unmet and is
tracked where it belongs, at 1.11; nothing in the CLI starts, stops or supervises
a `runnerd` (2.6); and **no part of this build should be read as a strategy
running unattended on a host nobody configured** — a Runner without a driver
accepts a create and writes a durable job nothing will advance, which is why
`create` reports `driving` and exits `UNAVAILABLE` when it is false.

## Status vocabulary

| Marker | Meaning |
| --- | --- |
| **DONE** | Public path and its failure/recovery behavior work, with tests. |
| **PARTIAL** | Works for a stated subset; the gap is named. Never report as done. |
| **SEAM** | An interface exists; no implementation ships. Not a capability. |
| **TODO** | Not started. |
| **BLOCKED** | Needs a backend capability or an undecided product question. |

A **SEAM** must never be described to a user as support for the thing it is a
seam for. The SDK has two, and both are correctly disclosed in
`packages/sdk/README.md`.

## Contract sync

`packages/sdk/src/contract.ts` and
`bucket-backend-mono/apps/waterx/src/predict/agent-api/agent-api.contract.ts`
were diffed in full at SDK `HEAD` and backend `f14d2a5f`. Below the file header
(SDK lines 1–14, backend lines 1–8, which differ only in the vendoring notice)
they are byte-identical. No drift. The backend commits since the last re-diff
(`5ab9d9e4`, `f14d2a5f`) added metrics and a performance read whose contract
types were already vendored; the file itself did not move.

**This commit changes no wire surface.** The durable job store is local by
decision, not by omission: the backend stores no job, no target and no
conditional order (ADR-0001 §7), so a durable strategy is a client-side fact and
there is nothing for the server to learn about it. Nothing under `packages/runner`
issues a request today, and `packages/sdk/src/contract.ts` is untouched.

**The contract's runtime values are now reachable from the entry point.** They
were not: `packages/sdk/src/index.ts` re-exported `./contract.ts` with
`export type *`, which carries no runtime binding, and the `exports` map admits
only `.`, so a deep import of the built module is blocked as well. Fourteen
values the SDK already holds — `PREDICT_AGENT_API_ROUTES`,
`IDEMPOTENCY_KEY_HEADER`, the seven `PREDICT_QUOTE_*` event names and caps,
`PREDICT_EXECUTION_STREAM`, `PREDICT_AGENT_STREAM_NAMESPACE`,
`PREDICT_STREAM_READY` and `RETRYABLE_PREDICT_AGENT_ERROR_CODES` — were
therefore invisible to every installed consumer, who had to hard-code a copy of
the route it was about to call or the cap a stream enforces. A copy is what
drifts from the contract this whole section exists to keep synchronised. Fixed in
`packages/sdk/src/index.ts` and held by `packages/sdk/tests/entry-point.test.ts`
(3), which imports through the entry point rather than the module so the test
fails the way a consumer would. This is additive: no export was removed or
renamed.

**The backend moved first again, for B3.** The quote stream (backend `2aedb8d8`)
adds `PREDICT_QUOTE_*` events, `PredictQuoteTopic`,
`PredictQuoteSubscribeMessage`, `PredictQuoteSubscriptionFrame`,
`PredictQuoteRejection(Reason)`, `PredictQuoteFreshness`,
`PredictQuoteStreamFrame` and `PredictQuoteHeartbeatFrame`, and widens
`PredictQuoteQualityFlag` with `INDICATIVE_ONLY`, `POLLED_UPSTREAM` and `STALE`.
The full semantic diff is **one** replaced line — that union — plus additions;
nothing was removed, renamed or retyped, and the union already carried a
`(string & {})` escape, so a client built against the previous contract still
compiles and still parses every response. As of this commit the SDK **consumes**
it: `packages/sdk/src/quote-stream.ts` speaks the protocol against these vendored
types, so the compiler is what catches the next drift (2.3).

**The backend moved first, three times.** B1 and B2 could not be closed on the
client side — one needs a text index the catalog endpoint did not expose, the
other needs a risk profile that only an owner-authenticated controller could
read — so the wire surface changed at `7ecad3f3` (`?search=` plus
`ListMarketsResponseBody.resolution`; `GET
accounts/:accountId/effective-limits`). B6 followed at `2e247fc4`
(`PredictAgentListQuery.cursor` on the three account reads, `nextCursor` on their
response bodies), because paging is a property of the query the database runs and
no client can synthesise it. Each time the complete contract body was re-vendored
here in one commit alongside the SDK method, the CLI commands, the schema entries
and the tests. Every change is purely **additive**: no field was removed, renamed
or retyped, so a client built against the previous contract still compiles and
still parses every response.

`nextCursor` is declared **optional**, which is the whole reason a client can tell
an old server from an exhausted history. If it were `string | null` a server that
never wrote the key would deserialise as `undefined` and read as "no more pages",
and a caller reconstructing its full history would stop early while reporting
itself complete. Absent means unknown; `null` means proven exhausted.

The write plane did not change this. It composes `createOrder` → sign →
`submitOrder`, `executeMany`, `getExecution` and `waitForExecution` exactly as the
SDK already implemented them; the approval token, the delegation scope and the
signing gate are **local** constructs that never appear on the wire.

## 1. Current SDK state — verified

| Capability | Status | Evidence |
| --- | --- | --- |
| Personal-message auth + token handling | DONE | `packages/sdk/src/client.ts:289`, `packages/sdk/src/signer.ts` |
| Bounded automatic re-authentication | DONE | `packages/sdk/src/session.ts`, `packages/sdk/src/transport.ts:89`, `packages/sdk/tests/reauthentication.test.ts` (14). Single-flight mint, compare-and-swap token replacement, one retry per request, `401 UNAUTHENTICATED` only, replay keeps exact bytes and key. |
| Executable quote | DONE | `packages/sdk/src/client.ts:314` |
| Market catalog list + get | DONE | `packages/sdk/src/client.ts:658`, `packages/sdk/src/client.ts:710` |
| Protected market order (create → sign → submit) | DONE | `packages/sdk/src/client.ts:339` |
| Terminal wait + REST reconciliation | DONE | `packages/sdk/src/client.ts:733` (public `waitForExecution`), `packages/sdk/tests/client.test.ts` |
| Terminal facts: fill, fee availability, remaining allowance | DONE | `packages/sdk/src/execution-facts.ts`, `packages/sdk/tests/terminal-result.test.ts` (14). Timeout returns `timedOut: true` with the execution id instead of throwing; absent fee is a reason, never zero; allowance is `undefined` off a non-terminal read and for an agent with no risk profile. |
| Stable idempotency across retries | DONE | `packages/sdk/tests/client.test.ts` |
| Server-driven retry policy | DONE | `packages/sdk/src/transport.ts`, `packages/sdk/src/errors.ts` |
| Exact decimal comparison | DONE | `packages/sdk/src/decimal.ts`, `packages/sdk/tests/wait-for-price.test.ts` |
| `executeMany` independent legs + STOP/CONTINUE | DONE | `packages/sdk/src/client.ts:388` |
| Allowance / positions / executions / fills reads | DONE | `packages/sdk/src/client.ts:554`–`:657`. The three history reads take `{ limit?, cursor? }` and return `nextCursor` (B6). |
| Keyset paging over account history | DONE | `packages/sdk/src/pagination.ts`, backend `2e247fc4`. Opaque row-anchored cursor over `(created_at, id)` for executions, `(filled_at, id)` for fills, `(created_at, id)` for positions, so a page boundary holds while new rows land at the head. `hasMorePages` is three-valued — `true` / `false` / `null` for "the server did not say" — and `isExhausted` is true only on an explicit `null` cursor, which the server proves by reading one row past the page. A malformed, edited, foreign-list or unowned cursor is refused as `INVALID_INPUT`, never ignored (B6). |
| `waitForPriceAndExecute` in-process trigger | PARTIAL | `packages/sdk/src/client.ts:444`. Correct trigger, fresh re-quote, re-verify, one submission — and that holds whether the trigger came from a poll or from a streamed frame. Still in-process only: it dies with the process, and is not durable. |
| Execution stream | DONE | `packages/sdk/src/execution-stream.ts`, `packages/sdk/tests/socket-execution-stream.test.ts` (35) + `execution-stream.test.ts` (13). `executionStream: 'native'` opens the official `socket.io-client` on the server's private namespace with the client's own session; the `ExecutionStream` seam remains for tests and custom transports. The default path still polls, and terminal state is **always** REST-confirmed — see 2.2/2.4. |
| Quote stream | DONE | `packages/sdk/src/quote-stream.ts`, `packages/sdk/tests/quote-stream.test.ts` (19) + `quote-stream-trigger.test.ts` (9). `quoteStream: 'native'` opens the official `socket.io-client` on the server's private namespace with the client's own session (`forceNew`, so it never multiplexes onto the execution stream's socket); the `QuoteStream` and `PriceWatcher` seams remain for tests and custom transports. Reconnect re-subscribes with `resume: true` and treats the `gap: true` snapshot as the whole recovery, `seq` is per (connection, topic) and never persisted, a heartbeat watchdog invalidates cached prices and rebuilds a silent connection, and everything else — no frame yet, `stale`, gap, rejection, degraded — falls back to `POST /quotes`. The trigger is still only a trigger: the order is priced off a fresh executable quote and re-checked against the target before submission. Default remains polling. |
| Server-side market resolution | DONE | `packages/sdk/src/client.ts` (`searchMarkets`), backend `7ecad3f3`. `?search=` matches published aliases server-side and returns `resolution` (`RESOLVED` / `AMBIGUOUS` / `NOT_FOUND`); `marketId` is non-null only on exactly one match, and `matchCount` is counted before `limit` truncates the page. A server that answers without a `resolution` reads as `NOT_FOUND` — the client withholds an id rather than inferring one (B2). |
| Agent-readable effective risk limits | DONE | `packages/sdk/src/client.ts` (`getEffectiveLimits`), backend `7ecad3f3`. `GET accounts/:accountId/effective-limits` returns the limits, the rolling-window usage, the allowance, the on-chain delegation and the blockers. **Read-only**: writes stay owner-authenticated (ADR-0003), so an agent credential can see its mandate and cannot raise it. `limits: null` is denial, not an unlimited default; a `null` delegation permission means the chain read failed, not that it was denied (B1). |
| Agent performance read | DONE | `packages/sdk/src/client.ts` (`getPerformance`), backend `5ab9d9e4`. `GET accounts/:accountId/performance`, optionally narrowed by `strategyId`, returning order outcomes, rejection reasons, realized PnL and the excluded populations. The client passes the figures through untouched: it computes no rate, sums no money and turns no `null` into a `0`, because a second authority on a win rate is a wrong one. The scope (`API_ATTRIBUTED_ONLY`) is part of the returned document, not a caveat in prose. `packages/sdk/tests/client.test.ts` (3 cases) covers the unfiltered call, the strategy filter, and null rates surviving as null. |

## 2. Phase 0 — spec freeze and threat model

| # | Item | Status | Note |
| --- | --- | --- | --- |
| 0.1 | Adopt `AGENTS.md` + runtime plan | DONE | This commit. |
| 0.2 | ADRs for the remaining open decisions | DONE | ADR-0002 (D-05), ADR-0003 (D-13), ADR-0004 (D-18), ADR-0005 (D-22); ADR-0001 records the confirmed baseline. |
| 0.3 | Checked implementation backlog | DONE | This file. |
| 0.4 | Runner trust boundary + crash/replay threat model | PARTIAL | Three of the five crash points are modelled **and enforced**, with the table written out in the header of `packages/runner/src/recovery.ts` and asserted twice — as a table through `classify`, and end to end against a database that is closed and reopened (`packages/runner/tests/recovery.test.ts`, 23). Crash between key persist and create → re-arm reusing the *same* key; crash between create and submit → `UNKNOWN_PENDING`, never a re-send, and now resolvable: `reconcileJob` reads the recorded execution back and admits only what the server reported (`reconciler.test.ts`, 15). The one crash point that stays unresolvable is a crash *inside* the create, where no execution id was ever recorded — that needs a backend read this API does not have (B9), and it is reported as `INCONCLUSIVE` rather than guessed. Duplicate Runner instances on one store → lease fencing tokens, so the superseded writer fails `LEASE_LOST` rather than interleaving (`store.test.ts`, 37). The gaps: **signer unavailable mid-job** is now modelled at the pass level — a refusal or an unavailable keystore leaves the created execution unsigned, records the reason on the leg as `signRefusedCode`, and hands the job to the reconciler rather than calling the order failed (`tests/strategy-driver.test.ts`) — but not yet against a real keystore process that dies mid-request, and **clock skew vs `expiresAt`** is untouched — recovery expires a job against an instant its caller supplies, and nothing yet reconciles that instant against the server's. |
| 0.5 | Job state machine specification | PARTIAL | Executable rather than prose: `packages/runner/src/state-machine.ts` is the table, and `tests/state-machine.test.ts` (9) asserts its properties over the whole edge set rather than the handful a feature used — no in-flight state may reach `CANCELLED`/`EXPIRED`, `UNKNOWN_PENDING` has exactly one exit, only `RECONCILING` may reach `FILLED`, terminal states are sinks, and every `WRITE`/`SIGN` state requires a persisted leg. The store enforces that last one at runtime (`NO_IDEMPOTENCY_KEY`). Includes `PAUSED` (ADR-0004) and `UNKNOWN_PENDING`. `driveJob` now enters and leaves `PAUSED` on a market verdict (2.9), so every state in the table is reachable by an implementation rather than only by a test. |
| 0.6 | CLI command + schema prototype | PARTIAL | Schema half is done (1.2): commands, inputs and the `implementation` mapping are fixed and validated. The CLI half now covers the read plane *and* the market-order write plane (1.3–1.8). The two-host discovery spike required by the plan's exit criteria is now done (3.1, 3.2): `tests/workspace.test.ts` issues one `market.quote` intent through the generic function-calling dispatcher and through the MCP `tools/call` method and asserts the argv handed to the command core is **identical** — and since both front doors spawn the same CLI binary rather than importing anything, the SDK request below it is the same request by construction. What remains under this item is that the spike is in-process against an injected invoker: no model host has driven either adapter, and no intent from one has reached a server (1.11). |
| 0.7 | Quote WS protocol + achievable SLO | BLOCKED | Backend. Upstream feed is ~2 s polling; SLO must precede any real-time claim. |
| 0.8 | Testnet provisioning + owner onboarding runbook | PARTIAL | The runbook and the checkable gap list exist; **most of it is now provisioned, by hand, on one machine.** Settled 2026-08-19/20: base URL, environment label, agent wallet, signer command, `defaultAccount` — which was **discovered rather than supplied**, via `GET /account?owner=`, correcting this file's earlier assumption that an operator hands it over — `ownerAddress`, the on-chain `delegation` (granted from a browser wallet to a dedicated agent wallet, predict permissions 9), and the `ownerRiskProfile` (written directly to the local store on the owner's explicit instruction, because the owner-authenticated endpoint needs an owner session this runtime deliberately cannot hold — ADR-0003 §1 stands, and that write was an owner action performed by an operator, not by the harness). Still unprovisioned: `runner` and `runnerRestart`, which is why steps 17–20 have not run. **None of it is reproducible by anyone else**: it lives in one local Postgres and one local backend, so the runbook this item asks for is still unwritten. `packages/e2e/src/gaps.ts` names ten gaps. The original seven — base URL, environment label, agent wallet, signer command, default account, delegation, owner risk profile — are unchanged and not restated; WP11 adds the three the durable half needs: `ownerAddress` (whose account a strategy trades), `runner` (a Runner answering its socket **and driving**) and `runnerRestart` (the operator's own way of restarting it). Each carries who supplies it, how, and the command that would settle it; `packages/e2e/README.md` carries the two-actor sequence. `packages/e2e/tests/gaps.test.ts` (9) fails if a gap loses its supplier, if a gap can be checked before a server is reachable, if the two **owner-authenticated** entries (`delegation`, `ownerRiskProfile`) stop saying that this pipeline must not attempt them, or if the restart gap ever grows a `pkill`/signal/pid instruction — this repository must not construct a way to stop a process it did not start. Those two owner-authenticated entries are a human task by ADR-0003 §1 and are not automatable here by design. Verified against this machine: `describe` reports no base URL, no label, no wallet, no signer and no account; `strategy list` gets `RUNNER_UNREACHABLE`; and no owner address or restart command was supplied — so eight gaps are MISSING and both owner-authenticated gaps are UNCHECKED, *not* denied, because no authenticated read happened. |

**Phase 0 exit criteria** (from the plan): one normalized intent produces an
identical SDK request through the CLI and through one tool adapter, and every
secret-custody and approval boundary has a named owner. The first half is now met
and asserted (0.6, 3.2). Still not met overall — 0.4, 0.5, 0.7 and 0.8 are open.

## 3. Phase 1 — universal one-shot interface

| # | Item | Status | Depends on |
| --- | --- | --- | --- |
| 1.1 | pnpm workspace split: `sdk` / `cli` / `runner` / `mcp` | DONE | `pnpm-workspace.yaml`, `packages/*/package.json`, `tests/workspace.test.ts`. SDK moved to `packages/sdk` with its published entry points unchanged; `schema` added; `cli`/`runner`/`mcp` were private, source-free reserved boundaries at the time, and all three are implemented now (`mcp` by 3.2, alongside `packages/adapters`). Dependency direction and published-package hygiene are enforced by test, not convention. |
| 1.2 | Versioned runtime command schema (single source of truth) | DONE | `packages/schema/src`, emitted to `schemas/v1/agent-commands.json`; ADR-0006. Eighteen commands, runtime-validated by `validateCommandInput` with no coercion; `packages/schema/tests` (71) cover the validator subset, unsupported-keyword rejection, every published example, BUY/SELL unit and position agreement, decimal/price/address patterns, and byte-for-byte artifact drift. `tests/workspace.test.ts` additionally fails if a contract command is not backed by an AVAILABLE capability, so the contract cannot advertise what no surface runs. |
| 1.3 | Consistent JSON envelope, symbolic error codes, exit codes | DONE | `packages/cli/src/{envelope,exit-codes,errors}.ts`; `packages/cli/tests/envelope.test.ts` (13). Exactly one parseable document on stdout on every path including an unresolvable command; usage prose on stderr only; the exit code derived from the server's own symbolic code rather than the HTTP status; `retryable` copied from the server, never re-derived. The code table is published by `describe` so a host need not hard-code it. |
| 1.4 | `describe` | DONE | `packages/cli/src/commands/describe.ts`; `packages/cli/tests/discovery.test.ts` (14). Answers with no configuration, no signer and no network. Reports the policy in force, its source, and that an approval token **is not authentication**; reports `signer.canSignTransactions` from the policy rather than from intent. `serverCapabilities.source` is `STATIC` — this build's own claim, not something the server advertised (3.3/B7 is still blocked), and it is labelled as such rather than presented as negotiated. |
| 1.5 | `doctor` | DONE | `packages/cli/src/commands/doctor.ts`; `packages/cli/tests/doctor.test.ts` (7). Config, signer, reachability, authentication, catalog read, allowance read, and `writePlaneCheck`. The write-plane check never places an order — a diagnostic that trades is one nobody can safely run — so it SKIPs under `read-only` and `interactive`, and under `delegated-auto` it FAILs the one write-blocker knowable without trading: a scope whose `notAfter` has already passed. A check that could not run is SKIP, never PASS, and the command exits with the **first failing check's own code** so a rejected token exits 4 rather than 70. Signs the login challenge as a personal message only. |
| 1.6 | `market` / `account` / `order` commands | DONE | Read plane: `market list/search/get/quote` and `account status/allowance/risk-limits/positions/executions/fills/performance` (`packages/cli/tests/read-plane.test.ts` (29), `input.test.ts` (16)). `account performance` reports the server's figures unchanged and states the scope beside them: `API_ATTRIBUTED_ONLY`, rates that stay `null` rather than becoming `"0"` over an empty denominator, and `excluded.claimedPositions` named as the population that biases `winRate` downward. Write plane: `order preview/execute/execute-many/get/reconcile` (`packages/cli/src/commands/order.ts`, `packages/cli/tests/write-plane.test.ts` (34)) — an ambiguous intent (wrong unit for the side, both units, neither, a SELL naming no position, a BUY naming one) is refused with exit `INVALID_INPUT` (2) and **zero network and signer calls**, because guessing the unit trades the wrong thing; a timed-out wait is `ok: true` with exit `AMBIGUOUS` (11) plus a reconcile instruction, never a resubmission; `execute-many` legs succeed, fail and skip independently and the envelope says `atomic: false`. `market search` reports the **server's** `resolution` verbatim and exits `AMBIGUOUS` (11) with `marketId: null` unless exactly one market matched — it never matches text locally, and a truncated page is never read as a unique answer. `account risk-limits` reads the mandate and cannot write one. `market history` and `order cancel` are still refused by capability negotiation with a symbolic reason and **zero network calls**. Two defects on the reconcile path are fixed and regression-tested: the ambiguous-write recovery instruction printed `--execution-id`, a spelling that exits `USAGE`, so the one command a caller runs while holding an order of unknown outcome did not run — the test now *executes* the printed string rather than matching it; and `order reconcile --timeoutMs` was bounded by the invocation timeout instead of the timeout it was given, so a long wait was aborted early. Durable/conditional order commands are 2.8, not this item. |
| 1.7 | `order preview` as a first-class command | DONE | `packages/cli/src/commands/order.ts` (`runOrderPreview`); `write-plane.test.ts`. Resolves the market server-side, mints a fresh quote, normalizes the leg, computes the price-protection bound (`CEILING` for BUY, `FLOOR` for SELL) and the effective worst price, runs the same policy engine `execute` runs, and returns the approval token with the exact `--approve …` string. Signs nothing and places nothing. Outside `read-only` it now also reads the **effective mandate** alongside the quote and reports `riskLimits`, `blockers`, `delegation` and the hour already used, so a caller sees what would refuse the write before signing — a report, never local enforcement. Nothing is synthesized: unread limits say `NOT_READ`, an account with no mandate says `NO_RISK_PROFILE` (B1). |
| 1.8 | Signer provider interface + keystore/keychain/KMS | PARTIAL | ADR-0001 §7. **Three providers now exist, and between them both policy modes have one.** The **external-command** provider is the interface both runtimes use (`packages/cli/src/signer.ts`, `packages/cli/tests/signer.test.ts` (21)): no key enters the process; the request is versioned and typed, so `PERSONAL_MESSAGE` and `TRANSACTION` are distinguishable by the key holder and never interchangeable; a non-zero exit, non-JSON stdout, a missing `signature` or a timeout is `SIGNER_FAILED`, never a fabricated signature; `signTransaction` throws before a child is spawned under `read-only`, and otherwise spends a `SigningGate` permit **before** the child runs. **`packages/signer-browser`** (13 tests) serves `interactive`: a single-use loopback page, no key anywhere in this workspace, a TRANSACTION decoded in Node (`Transaction.from(bytes).toJSON()`) so the wallet renders it, signing **without executing** because a sponsored order's second signature is the server's, and a refusal — naming what it found — for an address the wallet does not hold. **`packages/signer-keystore`** (14 tests) serves `delegated-auto`, which had no provider at all: an ssh-agent-shaped pair, `agent` unlocked once (scrypt N=2¹⁷ → AES-256-GCM, GCM's tag *is* the passphrase check so a wrong passphrase and an altered file answer identically) holding the key in memory, and `sign` spawned per request as a thin client over a `0600` socket in a `0700` uid-owned directory, with a per-start token compared in constant time and `sun_path` length refused by number rather than surfacing as `EINVAL`. Verified end to end against the local backend: `doctor` reaches `failed=0` — configuration, signer, reachability, **authentication** and catalog — with **no human interaction at all**. Neither signer decides *whether* to sign; that stays in `policy.ts` and the job snapshot, because a signer that enforced policy would be a second place for the rules to live. Both carry `@mysten/sui`, which is why they are their own packages (`tests/workspace.test.ts` PROVIDERS) and why the runtimes reach them as **processes**, never as libraries. **The gaps: OS keychain and KMS are still unbuilt, and the keystore's trade is real** — a decrypted key sits in one process's memory for as long as it runs, which is strictly weaker than the browser provider and is the price of signing while nobody is watching. The README says so, and says to load a *delegated agent wallet* rather than the account owner's key, so what is resident is bounded by the on-chain permission bits and the risk profile. |
| 1.9 | Automatic re-auth preserving key and exact bytes | DONE | `packages/sdk/src/session.ts`, `packages/sdk/src/transport.ts:89`, `packages/sdk/src/errors.ts:isUnauthenticated`; `packages/sdk/tests/reauthentication.test.ts` (14) covers replay under a fresh token with identical bytes and key, a token dying between create and submit, five concurrent 401s minting one session, the bound (one re-auth, then the error), a fresh signed challenge per mint, pre-expiry rollover, `autoReauthenticate: false`, and the rejections it must **not** retry (`403 DELEGATION_REVOKED`, `401 SIGNATURE_INVALID`, `409 IDEMPOTENCY_KEY_REUSED`). At the SDK layer only — 1.8's signer provider still gates the CLI/Runner path, where the challenge is signed outside this process.  **Across invocations, a session is now reused rather than re-minted**: `packages/cli/src/session-cache.ts` + `tests/session-cache.test.ts` (11). Every CLI run is a fresh process, so without this the runtime authenticates once per command — and with the browser-wallet provider (1.8) that is a human interaction per `market list`, which makes `interactive` unusable in practice rather than merely tedious. The cached token is the server's own, kept under the rules this repository already applies to the Runner's bearer token: a **0600 file in a 0700 uid-owned directory**, neither read nor written when that cannot be proven; keyed by **base URL and agent wallet**, so a token minted against one deployment is never offered to another; carrying the server's own `expiresIn` turned into an instant and discarded 30 s early, because a response with **no** stated lifetime is not cached at all rather than reused until something rejects it mid-order; written through a temporary file and renamed, so a crash leaves the previous session rather than a truncated one; and registered with the redactor the moment it is read. Every doubt — unparseable, expired, world-readable, another wallet's — costs one signature, which is the cheap side of that trade. Absent a uid or a way to write 0600, the cache disables itself rather than falling back to anything weaker. |
| 1.10 | Secret redaction across stdout, logs, errors, job store | PARTIAL | CLI streams DONE: `packages/cli/src/redact.ts`, `packages/cli/tests/secrets.test.ts` (7), plus the write-plane case that asserts no signature, transaction bytes or token reaches either stream on a successful order. Registered secrets are replaced on the serialized stdout document *and* on every stderr line, including when the server echoes a token back inside its own error message; a credential-shaped config file key is refused naming the key and never the value; the signer is reported by executable base name only. The job store half is now covered by **refusal rather than redaction**: `packages/runner/src/secrets.ts` rejects a secret-shaped property before it reaches the file (`SECRET_REJECTED`), and the store records a SHA-256 digest of a request instead of its bytes. Refusal, not masking, because a redactor has to know the exact secret and the risk here is the field nobody anticipated. The gap: the two sides are unrelated code — the CLI's registered-secret redaction and the store's refusal list do not share a definition of what a secret is. The CLI now registers **both** credentials it holds, not just the session token: `openRunnerSession` takes an `onSecret` seam and hands the Runner's bearer token to the same redactor the moment it comes off disk, before it is written to any socket. That token authenticates to the process that holds the signer, so it is the more dangerous of the two, and `packages/cli/tests/strategy.test.ts` now asserts it reaches the `hello` frame and neither stream — including when a Runner quotes it back inside its own refusal and when it appears in a diagnostic. Both of those assertions fail if the registration is removed. The Runner now logs (2.6): `runnerd` writes one JSON diagnostic per line to **stderr**, never stdout, and `tests/daemon.test.ts` asserts the IPC token appears in no event and no reply. That is a checked absence for the one secret the daemon currently holds, not a redactor. The daemon now holds more of them and each is handled by construction rather than by a redactor: the start-up diagnostic prints the keystore by executable **base name** only (a full argv can name an operator or a token slot) and prints no token because the configuration cannot carry one; the signer's child **stderr** is forwarded to an `onDiagnostic` channel while its stdout — which holds the signature — is not; and a config refusal names a credential-shaped key by path and never by value. Asserted in `packages/runner/tests/config.test.ts` (38) and `tests/signer.test.ts` (25). The gap is unchanged: three unrelated definitions of "secret" (the CLI's registered-secret redactor, the store's refusal list, the config's key pattern) with no shared source. |
| 1.11 | Testnet quickstart + real E2E | PARTIAL | The harness is built and executable, and **all twenty steps have now run against a live server** (2026-08-19/20). What that run was, precisely: a backend built from `codex/waterx-predict-agent-runtime` and started **locally** — the agent API is deployed nowhere, so no other environment could have served it — against the **staging ClickHouse** for market data and a **relayed staging Redis** for quotes, on **Sui testnet**. The chain half was not simulated: two delegations were granted on chain by the account owner from a browser wallet (`3Bcx6KChuYeee1BrmWNsx7uTvgoiwVTpAQ8X1U7hY8QZ` being the one to a dedicated agent wallet), and `order execute` submitted a real transaction (`8t6Nhx2wVgQ4rJSKZfGkgy3s4mj6CJ5EEL9kim6Vp99s`) that came back **CANCELLED with no fill** — price protection holding, `fee.available: false` with reason `NO_FILL_OBSERVED`, and `remainingAllowance` unchanged because nothing was spent. Steps 1–13 passed: describe, doctor (failed=0), market list/search/get/quote, order preview, order execute, terminal wait, order get, order reconcile, account positions/fills. `market search` answered `AMBIGUOUS` with `marketId: null` on forty matches, which is ADR-0001 §10 verified rather than asserted. Steps 14–16 followed: two independent quotes on opposite outcomes of one market, then a non-atomic `order execute-many` that reported `atomic: false` with **one leg SUCCEEDED and one FAILED** (`QUOTE_EXPIRED`, from the server) and `signatures: granted 2, used 1, unused 1` — the gate proving a failed leg spent no signature. The succeeded leg is this project's **first real fill**: 3 wxUSD for 63.402803 shares at `avgFillPrice` 0.047316 against a quote of 0.047317, a quote-to-fill deviation of 1e-6 and the first datapoint the SPEC's single-point risk has ever had; `fee.available: false` with reason `EMBEDDED_IN_PRICE`, which is a DIFFERENT reason from the earlier `NO_FILL_OBSERVED` rather than both collapsing to zero. Steps 17–20 ran against a configured `runnerd` (`delegated-auto`, signing through the keystore provider): `strategy list` answered `driving: true`, `strategy create` armed a PRICE-triggered job with a mandatory expiry that settled in **WATCHING** rather than filling, and `strategy cancel` answered `recorded: true, applied: true` into a terminal `CANCELLED`. **Step 19 was run with a real `SIGKILL`**, not a clean stop: the daemon was killed outright, restarted, and a DIFFERENT `instanceId` answered for the same job, still `WATCHING`, still non-terminal, with `openSideEffects: []` reported rather than assumed. **What this run does NOT establish:** it was not a deployed environment, the quote feed reached the server through a relay rather than its own publisher, and the run was driven by hand rather than by the harness end to end — so `packages/e2e`'s own verdict is still PARTIAL and its gap list still names what is unprovisioned. `packages/e2e` drives the *installed* binary as a subprocess — resolved through `node_modules` the way a consumer resolves it, not by importing the handlers — across **twenty** steps in two halves. The immediate half is thirteen: describe, doctor, market list/search/get/quote, order preview, order execute, terminal wait, order get, order reconcile, account positions/fills. The durable half is seven, added by WP11: two independent quotes on opposite outcomes, a non-atomic `order execute-many` whose reply must report `atomic: false` with two distinct execution ids and per-leg statuses, a `strategy list` that finds a **local** Runner naming its `instanceId` and saying it is driving, a `strategy create` arming a PRICE-triggered job with a mandatory expiry (a BUY ceiling far below the market, so it waits rather than fills), a `strategy get` **after the operator's own restart command has run** that must see the same `jobId` answered by a *different* `runner.instanceId`, still non-terminal, with its `openSideEffects` list present — and `strategy cancel`, which is deliberately ungated and last. Eight executable shell examples ship — `one-shot-entry`, `slippage-rejection`, `delegation-revocation`, `reconciliation`, `multi-leg-entry`, `target-exit`, `restart-recovery` and the whole `north-star-bot` — plus the stream example `packages/sdk/examples/watch-quotes.mjs`, which lives with the package that implements the stream because a subscription has no command shape. The last four shell examples and the stream example need a provisioned environment and a local Runner, and **none of them has been run against a server here**; what is proven is that each refuses with a named supplier, and that every command it prints exists with the flags it uses. Honesty is enforced by construction, not by a paragraph: a `NOT_RUN` step carries no evidence field, so "passed without running" is unrepresentable; a step whose evidence transport is `STUB` makes the entire report `INVALID`, so a mock cannot stand in for a live path; and only an all-steps-passed report exits 0. Each write needs its **own** opt-in **and** an environment label on the non-production allowlist — there are three capabilities, not one flag (`order` for one order, `multi-leg` for two at once, `strategy` for a job that can trade after the process exits), so agreeing to the smallest never authorizes the largest; an unlabelled deployment is treated as production and is never traded on. Proven by tests asserting the invoker never receives an `order execute`, `order execute-many` or `strategy create` argv under a `mainnet` label, and that each single opt-in permits exactly its own capability. Two further properties are enforced rather than promised: `strategy cancel` is **not** gated and runs last, so a run cannot end by arming a job and then declining to stop it — it reports `NOTHING_TO_DO` when this run armed nothing, and fails loudly when a cancellation was recorded but not applied; and the restart is the operator's supplied command run through `src/external.ts`, with no `pkill`, no signal, no pid lookup and nothing backgrounded — a restart that fails is `NOT_RUN`/`PREREQUISITE_NOT_MET`, because nothing was asked of the CLI. `packages/e2e/tests` (90) also lint every step's argv **and** every published invocation, including the examples and the READMEs, against the command contract, and execute each example with an empty environment to prove it refuses with a named supplier rather than a stack trace. **Actual result on this machine: PARTIAL, exit 10 — one step (`describe`) passed and nineteen could not run, with all ten gaps outstanding.** That is the harness working, not an E2E passing. Depends on 1.1–1.9, on 2.6's Runner daemon, and on 0.8's provisioning, which is a human task. |

On 1.2 and what it does **not** mean: `schemas/v1/agent-commands.json` describes
twenty-four commands, and the CLI now runs all twenty-four — but describing a
command is still not running one, and the guarantee comes from a test rather than
from this sentence. `tests/workspace.test.ts` fails if any contract command lacks an
AVAILABLE capability in `packages/cli/src/capabilities.ts`, so the contract
cannot drift back into advertising an intent no surface performs.

The v1 command set covers only what a surface can actually perform. `describe`,
`doctor` and `command-schema` are in it under an `implementation` union whose
`runtime` arm marks a command the runtime answers locally rather than through an
SDK method (ADR-0006). `market history` and `order cancel` are **absent on
purpose**: a command entry is precisely what an adapter turns into an advertised,
callable tool, so listing one would present a capability that does not exist.
Both are refused by name at the CLI with a symbolic reason and no network call,
so an agent asking for them gets an answer instead of silence.

`market search` and `account risk-limits` were absent for the same reason and are
now present, because the **server** grew the endpoints behind them (`7ecad3f3`).
The five `strategy.*` names moved the same way and for the same kind of reason,
except that what grew was here rather than there: they were absent while no
surface could honour one, and they are present now that the Runner executes a job
and the CLI can hand it one. The rule is unchanged — a name appears when
something can perform it, never when a client learns to approximate it — and it
is the reason the `implementation` union grew a third arm, `runner`, instead of
these being filed as `sdk` calls they are not.
That is the only admissible reason for a name to move in this direction, and
`packages/schema/tests/document.test.ts` says so at the point where the old list
lived: a command must never be added because the client learned to approximate
one. Approximating `market search` locally would mean returning a `marketId` this
CLI picked out of a truncated page, which ADR-0001 §10 forbids.

On the write plane and what it does **not** mean: an approval token binds one
exact intent, and it is **not authentication**. It is a digest any caller holding
the same order fields can compute, so it proves the order was not altered between
preview and execute — never that a person saw it. Human-in-the-loop is a property
of the host that decides whether to pass the token back; the CLI can only
guarantee that the order executed is the order approved.

On 1.9 and what it does **not** mean: the no-duplicate-execution guarantee is
proven in-process, against a fetch double, for a write the SDK itself replays.
It has not been exercised against a real server (1.11), and it says nothing about
a crash between the create and the submit — that is 0.4's threat model and 2.5's
durable store, because recovering from it needs the idempotency key to have
outlived the process.

**Phase 1 exit criteria**: an onboarded user gets a shell-capable agent to its
first testnet trade in 15 minutes, and a token expiring mid-flow causes no
duplicate execution. **Not met.** Neither half has been observed: no testnet
trade has been placed by this runtime, and the no-duplicate-execution property
is proven in-process against a fetch double only. 1.11 now provides the
instrument that would measure both; provisioning it is 0.8, and two of its ten
gaps are owner-authenticated actions no automation here may take.

## 4. Phase 2 — streaming and durable strategies

| # | Item | Status | Depends on |
| --- | --- | --- | --- |
| 2.1 | Backend quote WS | DONE (backend `2aedb8d8`) | Socket.IO on the existing private namespace `/agent-api/v1/predict`: `predict.quotes.subscribe` / `.unsubscribe` → a per-topic `predict.quotes.subscription` ack, a `predict.quotes.v1` SNAPSHOT per accepted topic, UPDATE only when a price moved, and a 15 s `predict.quotes.heartbeat` carrying each topic's `seq` and `stale`. 32 topics per connection, 60 messages per rolling minute, rejection per topic (`INVALID_REQUEST` / `UNKNOWN_MARKET` / `MARKET_CLOSED` / `NOT_QUOTABLE` / `SUBSCRIPTION_LIMIT` / `RATE_LIMITED`). **No replay exists and none is claimed**: this is a state feed, `seq` is per (connection, topic) from 1, the snapshot *is* the recovery, and `resume: true` returns it with `gap: true`. **Not low-latency, and labelled as such**: the server polls an upstream cache every ~2 s behind a publisher on a ~5 s cadence, so every frame carries `POLLED_UPSTREAM` plus `freshness.pollIntervalMs` / `staleAfterMs`, a past-TTL value publishes as `stale` with **null** prices, and an unstamped value reports `sourceAgeMs: null`, never 0. SLO measured as freshness — `value_age_seconds` P95 ≤ 8 s / P99 ≤ 15 s end to end, `delivery_lag_seconds` P99 ≤ 1 s for the WaterX-controlled portion. Tests: `predict-agent-quote-stream.service.spec.ts` (30) + `predict-agent-stream.gateway.spec.ts` (14, four of them the quote delegation and release), on a mock socket seam — no port, no live Socket.IO server. |
| 2.2 | Native execution stream client (replaces the SEAM) | DONE | `packages/sdk/src/execution-stream.ts`. `socket.io-client@^4.8.3` added as the SDK's only runtime dependency, justified in that file's header and allowlisted in `tests/workspace.test.ts`; loaded with `await import`, so a caller that never streams never loads it, and a pruned dependency degrades to polling instead of crashing at module load. WebSocket-only transport, because the server fans out per pod with no Socket.IO Redis adapter. Note what this buys: the outbox dispatcher publishes on a ~5 s tick, so it saves **requests**, not milliseconds. |
| 2.3 | Native quote stream client (replaces the SEAM) | DONE | `packages/sdk/src/quote-stream.ts`. Subscribes only after the ready frame proves the handshake was accepted, batches a burst of topics into one message against the 60-per-minute budget, and reports a `seq` break — from a frame or from the heartbeat, which is the only gap signal a quiet market has — without re-subscribing, because on a state feed a gap costs intermediate values and not the value now. A reconnect re-subscribes every held topic with `resume: true` and invalidates every cached price first. Two missed heartbeats rebuild the connection; a bounded number of silent windows, or of refused handshakes, gives up to REST polling via `onDegraded`. `MARKET_CLOSED` / `UNKNOWN_MARKET` / `INVALID_REQUEST` are terminal for the round and never re-asked; `NOT_QUOTABLE` / `SUBSCRIPTION_LIMIT` / `RATE_LIMITED` are retried on a timer. What it buys is **requests, not milliseconds** — one quote mint per trigger instead of one per tick (2.1: the server polls an upstream cache every ~2 s). Integrated into `waitForPriceAndExecute`: `pollIntervalMs` becomes a ceiling, the subscription is bracketed to the wait, and the order is still built on a fresh `POST /quotes` and re-verified against the target. |
| 2.4 | Gap detection, cursor persistence, REST reconciliation | PARTIAL | Implemented in the stream client: the cursor advances monotonically (BigInt, never rewound by a replay), rides every handshake including reconnects, and `gap: true` — plus a plain reconnect, which loses frames just as quietly — wakes every waiter to re-read over REST. `onCursor`/`cursor` expose the resume point for a caller that persists it. **A place to persist it now exists** — `saveCursor`/`readCursor` on the Runner's store keep a monotonic resume point across a restart and refuse a rewind (`CURSOR_REWIND`), tested through a closed and reopened database. The gap is now precisely locatable, and it is **not** in the Runner. A process that outlives an invocation exists and now opens a stream (2.6) — but the stream it opens is the *quote* stream, which has nothing to persist: it keeps no log, so its snapshot is the recovery and its `seq` is meaningless off the connection that issued it. The Runner deliberately does not open the **execution** stream, the one this item's cursor belongs to, because a terminal state is admitted only from an authoritative REST read (`getExecution`) and never from a frame; wiring an execution stream into it to give the cursor somewhere to go would add a second path to a terminal state, which 2.2 exists to prevent. So this item now waits on a consumer that streams executions — an embedding application or a future surface — rather than on the Runner. |
| 2.5 | SQLite/WAL durable job store behind a store interface | DONE | ADR-0001 §8, ADR-0007. `packages/runner/src/store.ts` is the engine-free interface; `src/sqlite/` is the implementation, on `node:sqlite` so no native addon or postinstall script runs inside the process that will hold the signer. `journal_mode = WAL`, `synchronous = FULL` (NORMAL may lose the last transaction, which here is routinely the idempotency key written immediately before the order that uses it), `foreign_keys = ON`, `STRICT` tables, forward-only migrations on `user_version`, and a newer schema refused outright (`SCHEMA_TOO_NEW`). Every method is exactly one transaction and none is exposed, so a half-applied "persist the key, then mark the state" is unrepresentable; every mutating job method takes a lease. What it holds: the job, its normalized intent, trigger, policy snapshot, `expiresAt`, per-leg idempotency key, reference and submission quote ids, execution id, both digests, the side-effect ledger, the stream cursor, instance heartbeats and an append-only transition log. What it refuses to hold: signatures, sponsored transaction bytes, tokens and key material — a secret-shaped field is **rejected** (`SECRET_REJECTED`), not redacted, and a request is recorded as a SHA-256 digest, which proves a replay is byte-identical and is useless to whoever can read the file. `packages/runner/tests/store.test.ts` (37), against a real file that is closed and reopened. |
| 2.6 | Runner daemon + local IPC | PARTIAL | ADR-0008. **The process, its socket, the job driver, the loop that calls it, the configuration it builds that loop's collaborators from and now the `strategy.*` commands that hand it one all exist; what is missing is a command an operator can type (2.8).** `packages/runner/src/daemon.ts` + `src/bin/runnerd.ts`: start-up order is asserted-directory → open store → register instance → recover → listen, so no client can observe a Runner that has not yet decided what its jobs are, and a failure at any step unwinds the ones before it. `src/supervisor.ts` renews leases and heartbeats on a timer, without bumping the fence, and aborts a job's signal both when it was fenced out and when a renewal failure has left the lease inside the safety margin. `src/ipc/` is the local surface: a Unix domain socket at `0600` inside a `0700` uid-owned runtime directory that is **asserted rather than repaired**, a 256-bit token reminted on every start and compared with `timingSafeEqual`, versioned NDJSON frames with a bound, an ordered per-connection queue, `sun_path` length checked, and a stale socket taken over while a live one is refused. The surface is `runner.status`/`jobs`/`job`/`cancel-job`/`shutdown` plus the five `strategy.*` commands; an agent command from the shared registry is refused `NOT_IMPLEMENTED` naming the missing pieces rather than answered, and `cancel-job` distinguishes `recorded` from `applied` so a cancel during a possible write never reports a stop that did not happen. Tests: `tests/daemon.test.ts` (25), `tests/ipc.test.ts` (48), `tests/supervisor.test.ts` (14), `tests/scheduler.test.ts` (15), `tests/prices.test.ts` (20), all in-process on an ephemeral socket or none at all. `UNKNOWN_PENDING` reconciliation now exists as `src/reconciler.ts` (`tests/reconciler.test.ts`, 18): it moves the job to `RECONCILING` **before** the read, so a second crash cannot leave a job that already learned the answer still claiming it does not know; it admits only a status the server reported (`REJECTED` → `FAILED`, `CANCELLED`/`EXPIRED` verbatim) and leaves `SUBMITTED`/`PENDING_FILL` alone rather than finalizing a live order on a clock; it resolves the open attempt and patches the leg in one transaction; and it refuses to call a submit successful without a digest or a status at or past `SUBMITTING`, because an execution `REJECTED` by risk before it was sent is a submit that did *not* take effect. Legs resolve one per pass and independently (ADR-0001 §15), and an unresolved leg is preferred by evidence rather than by index, so one ambiguous create cannot hide its resolvable siblings forever. A leg with **no attempt row of any kind** is checked before any execution is read — the one absence that proves something, because the row is committed before the request — and answers `REARMED` (nothing was ever sent: back to `WATCHING`, keys kept) or `REPLAYABLE` (a sibling's request left the process: the driver finishes the run under the key on disk). The create-phase case is unresolvable against this API (B9), so it reports `INCONCLUSIVE` and leaves the job visible. The **driver** now exists: `src/strategy/driver.ts` + `src/strategy/preflight.ts` (`tests/strategy-driver.test.ts`, 43) advance one claimed job by one pass and call the reconciler at the end of it. The **loop** now exists too: `src/scheduler.ts` (`tests/scheduler.test.ts`, 15) claims every runnable, unleased job each tick up to a `maxJobs` bound it *reports* rather than silently applies, drives each held job one pass in sequence, joins a concurrent `tick` instead of running a second pass over the same jobs — the guard that stops a tick interval shorter than a slow pass from minting two keys for one intent — skips a pass whose lease signal has already aborted, forgets a job on `LEASE_LOST`, and records a pass that threw against its job while the other jobs still get theirs. `stop` awaits the pass in flight, because a clean shutdown that abandoned a half-finished pass would manufacture the ambiguity the ledger exists to recover from, on purpose, on every Ctrl-C. The daemon takes a `driver` — gateway, `PriceObserver` and `StrategySigner` supplied together — and starts a scheduler only if it gets one; `driving` is read from whether that scheduler is ticking, never from a constant. The **price source** now exists too: `src/prices.ts` (`tests/prices.test.ts`, 20) puts a `PriceObserver` over the SDK's indicative quote stream. It subscribes once per market/outcome however many sides watch it, reads the ask for a BUY and the bid for a SELL through `streamTriggerPrice` so trigger and quote read the same book, and drops the cached frame on *every* event that means the feed can no longer prove it is live — stale, `GAP`, `DISCONNECTED`, `DEGRADED`, or a per-topic server refusal — because "nothing was observed" and "the market moved away" must not look alike to the executor. It accepts a `gap: true` resume snapshot as an answer, on the stated ground that every trigger here is a level test on current state rather than an edge test on a crossing, and records the gap for diagnosis rather than hiding it. It **does not fall back to `POST /quotes`**, unlike the SDK's `QuoteStreamPriceWatcher`: a `WatchKey` carries no size and that request requires one, so falling back would mean inventing a probe size and minting a priced, executable artifact to read a number. Subscriptions expire by disuse rather than by notification, so no cancellation, expiry, lost lease or crash leaks one, and `topics()` surfaces a topic gone permanently quiet under `DEGRADED` instead of leaving a strategy that waits forever looking healthy. The **signer** now exists too: `src/signer.ts` (`tests/signer.test.ts`, 20) signs sponsored bytes through a keystore command that holds the key, and refuses `interactive`, `read-only` and an unrecognized mode before spawning it (1.8). The **configuration** now exists too, which is what makes the shipped `runnerd` advance anything: `src/config.ts` (`tests/config.test.ts`, 38) resolves a runtime directory, a store path, a base URL, an agent wallet, a keystore argv and the bounds from the environment first and one JSON file second — no flags, because a process that runs for days should be configured by a document an operator can read back. It is **all-or-nothing**: a missing base URL, wallet or command yields `driver: undefined` and an ordered `gaps` list computed from the same values the driver would have been built from, so a diagnostic and a `driverGaps` reply cannot disagree. There is deliberately **no token setting, from anywhere** — a seven-day mandate outlives any token, so the Runner authenticates itself through the same keystore command — and a credential-shaped key at any depth in the file is refused naming the key path and never the value, before unknown-key checking, so `privateKey` is refused as a credential rather than as a typo. `src/runtime.ts` (`tests/runtime.test.ts`, 9) is the only place a `SchedulerDriver` is constructed: one `PredictAgentClient`, its **own** native quote stream pulled out through `client.quoteStream()` so REST and WS share one session and one token rollover, a `PERSONAL_MESSAGE`-only auth signer that throws on `signTransaction` — so the only road to an order signature in the process runs past the policy gate — and a lazily opened session that collapses concurrent opens into one mint, because a Runner that could only authenticate at boot would be a Runner whose durable jobs die of a deploy. `src/bin/runnerd.ts` wires it: config diagnostic (keystore by **base name only**), driver or named gaps, store, daemon, `driving`. `runner.status` now also carries a `prices` block — topics held, `degraded`, `silent`, and a row each — because a `DEGRADED` topic answers "nothing observed" for the life of the process and would otherwise look exactly like a quiet market. The socket surface now also serves `strategy.create`/`get`/`list`/`cancel`/`events` (`tests/daemon.test.ts`, 36; `tests/ipc.test.ts`, 52), delegating to the daemon's own `StrategyService` rather than re-deciding anything: the schemas describe shape only, so a client is refused by the named rule (`EXPIRY_REQUIRED`, `SIZE_AMBIGUOUS`, `POSITION_READ_UNAVAILABLE`) instead of by an anonymous schema violation, `strategy.cancel` *is* `runner.cancel-job` under the other name, `strategy.events` refuses an unknown job rather than answering the empty feed a library caller correctly gets, and a create answers with `driving`/`driverGaps` beside the job, because on an unconfigured Runner that job is real, durable and never going to move. The mandate it is admitted under is a new `policy` block in `src/config.ts` (`tests/config.test.ts`, 46) — mode, `maxOrderNotional`, `notAfter` — resolved at start-up and **never** from a request, so a socket peer cannot widen its own authority; an unrecognized mode is refused at start-up rather than at the first trigger, a ceiling that arrived as a JSON number is refused rather than round-tripped through a float, and the default is `interactive`, which `requirePolicyThatCanSignUnattended` refuses — so a half-configured Runner answers, recovers and arms nothing. `maxRunNotional` is now a setting, and enforced: `maxOrderNotional` bounded one order while nothing bounded a hundred of them, which is the shape of a strategy — orders placed with nobody present for any of them. It is committed to the store before the first write of each leg (`mandate_spend`, schema v2, keyed on (job, leg) so a replayed pass commits the same money once), totalled per mandate digest so a rewritten mandate starts its own budget, and refused as `EXCEEDS_POLICY_RUN_LIMIT` with nothing sent. The commit and the ceiling check are ONE transaction, because a ceiling read in one call and written in the next is a ceiling two passes both find room under. `runner.status` and the `strategy.create` reply both carry the ceilings and the committed total, since a cap an operator cannot read the balance of is one they learn about by having an order refused (`tests/store.test.ts`, `tests/strategy-driver.test.ts`, `tests/config.test.ts`). A *command* that speaks this protocol now exists (2.8), built exactly the way this row predicted it would have to be: the CLI still cannot depend on this package, so it carries its own client over a shared protocol descriptor, the way `SIGNER_PROTOCOL` is shared today. **Still missing:** the rest of daemon-side policy (2.7), and the daemon's own *lifecycle* from the CLI — nothing there starts, stops or supervises a `runnerd`, and nothing keeps the device awake, so an operator runs the binary and owns the machine's uptime. Nothing here has been run against a real server. No Windows fallback in beta (ADR-0002, ADR-0008). |
| 2.7 | Policy engine: `interactive` / `delegated-auto` / `read-only` | PARTIAL | ADR-0001 §9. Implemented and enforced for **one-shot CLI writes**: `packages/cli/src/policy.ts`, `packages/cli/tests/write-plane.test.ts` (34) + `signer.test.ts`. `--policy` may only narrow, never widen; `read-only` is enforced inside the signer; `delegated-auto` requires a file-only scope with mandatory per-order and per-run ceilings, an allow-listed account, side and market, and an unexpired `notAfter`, and every local check runs **before** any network read so an out-of-scope order costs nothing. The gap: no daemon-side policy and no policy evaluated across a durable job's lifetime — the daemon and the loop that drives its jobs both exist now (2.6), and neither evaluates a policy beyond what `preflight` reads off the job's own snapshot. The store snapshots the policy that admitted a job (`JobPolicySnapshot`), and `preflight` now performs **part** of the hour-later check: every driven pass re-reads the snapshot's `notAfter` and ends the job `POLICY_EXPIRED`, and applies `maxOrderNotional` to a BUY leg before it is quoted. The policy **mode** is now enforced across a durable job's lifetime, in three places that each catch what the others cannot: `normalizeStrategy` refuses a create under `interactive`, `read-only` or an unrecognized mode, so an owner learns at the prompt; `checkLocalPolicy` refuses on every pass **before any read**, so an unsignable job never watches a market or reserves allowance; and `createExternalCommandSigner` refuses before it spawns, so a path that skipped both still cannot produce a signature. `interactive` is refused rather than queued because a durable strategy fires when nobody is present to approve, and an unrecognized mode **pauses** rather than ends the job (ADR-0004). Tested as features in `tests/signer.test.ts` (20), `tests/strategy-intent.test.ts` and `tests/strategy-driver.test.ts`. The **per-run ceiling now exists on the Runner too**: `policy.maxRunNotional` is committed and enforced per mandate across jobs and restarts (2.6). It is deliberately a SECOND book rather than the CLI's: `tests/workspace.test.ts` forbids the Runner depending on the CLI, so one shared ledger would mean moving the ledger somewhere both may depend on — a change that needs its own ADR. Two books each bounding their own signer is sound; two books an operator believes is one is not, so `strategy create` now says on stderr when a local `maxCumulativeBuyAmount` does not apply to what the Runner will sign. The remaining gap is the rest of the scope — allow-listed account, side and market — which is still enforced only by the CLI's one-shot evaluator. |
| 2.8 | `strategy create / get / list / cancel / events` | DONE | `packages/runner/src/strategy/` — the command core, on the durable store: `create` normalizes an intent and writes a job, `get`/`list` summarize it (`list` filters by account, strategy and state, and returns summaries rather than fanning out per row), `events` merges the transition log with the side-effect ledger into one ordered feed with stable ids in which an unresolved attempt appears as `SIDE_EFFECT_BEGAN` marked `unresolved` rather than as silence, and `cancel` is the *same* implementation the IPC socket now delegates to, so `recorded` vs `applied` cannot drift between two surfaces. `pastExpiry` is reported as a fact about the clock, never as the `EXPIRED` state, because only a Runner holding the lease may end a job. Tests: `tests/strategy-service.test.ts` (14) against a real database that is closed and reopened, `tests/strategy-intent.test.ts` (33). **The command now exists, which was the last gap.** `packages/cli/src/commands/strategy.ts` + `src/runner-ipc.ts` (`packages/cli/tests/strategy.test.ts`, 21) — five contract entries, five handlers, and the CLI's own socket client. Three refusals are the point of it, and each is a place an operator loses money: a Runner that is **not driving** still accepts a create and writes a real durable job nothing will advance, so the envelope carries the job id (you need it) and the exit code is `UNAVAILABLE` (7) with `driverGaps` on stderr — `ok: true` and "watching" are not the same claim; a create whose **answer never arrived** is `CREATE_OUTCOME_UNKNOWN` and exit `AMBIGUOUS` (11), never a retry, because the Runner does not deduplicate creates (`strategyId` is an attribution label) and a second one arms a second strategy that also trades — deliberately over-reporting, since the cost is one `strategy list` and the cost of the other error is a duplicate position; and a **cancellation that was recorded but not applied** exits 11 rather than reporting the record as the stop, because an order already on its way to the chain cannot be recalled. Before the dial, the runtime directory is asserted `0700` and uid-owned and the socket path is length-checked, so a directory another local account can reach is refused with **zero socket traffic** rather than handed the operator's wallet addresses and sizes. The CLI decides nothing: `--policy read-only` is its one local refusal (a strategy trades later, unattended), and every other rule — expiry, sizing, position read, mandate — is the Runner's, carried out by its own symbolic code. The workspace edge stays refused: `tests/workspace.test.ts` (41) holds the CLI's `RUNNER_IPC_PROTOCOL` copy equal to the Runner's and runs the CLI's real frames through the Runner's real `decodeClientFrame`, which is what makes the duplication safe rather than hopeful. **The advertising is now honest about what a standard install contains, which it was not.** All five commands were `AVAILABLE` — true of the CLI half — while the daemon they talk to is `private`, unpublished and absent from every install path, so a stock setup answered `RUNNER_UNREACHABLE` five times and the printed remedy named a package that cannot be installed. `AVAILABLE` now carries a `requires` block (`LOCAL_RUNNER`, what it says, and a check that is runnable as printed and is tested to be), the projected tools tell a model the process is missing rather than unreachable so it does not retry an outage that is not one, and the CLI, MCP and adapter READMEs say the same thing. **The shipping now has a mechanism, and it is gated.** ADR-0029 (Proposed) makes the Runner a third operator artifact and the first OPTIONAL one: `releaseRefusal` still refuses all-or-nothing across the CLI and the signer — the setup sentence installs both — while an optional artifact whose ADR is not Accepted is left out of the release and named in the output, so an undecided daemon cannot hold back the CLI. Its own Node 24 floor (ADR-0007, unreopened) travels in its tarball, which is what lets npm refuse it to a Node 20 operator without refusing them anything else, and it is a SECOND install sentence rather than part of the first. Proven rather than described: `pnpm cli:bundle` packs all three with the SDK and the schema inside the Runner as it does the CLI, and `pnpm cli:bundle:check` now installs all three with npm alone and scripts off, then **starts the installed `runnerd` and reaches it from the installed CLI over its socket**, which is the one failure three separately-working tarballs would otherwise pass. `packages/runner/package.json` gained the `files` list the packer refused to ship without. **Nothing is released**: while ADR-0029 says Proposed the artifact is built and checked and withheld, and the advertising above still says the Runner is not installed — flipping `Status` is what changes both, in one commit. **What this does not mean:** no strategy has been created against a live server or watched to fill — that is 1.11, unchanged, and the phase exit criteria still turn on it. A configured `runnerd` will advance a created job — `driveJob`, `JobScheduler` and the configuration that builds their collaborators all exist (2.6), and a strategy created under `interactive` or `read-only` is refused outright rather than written and left unadvanceable (2.7). All five commands are reachable from outside the process, over the IPC socket, delegating to the same service (2.6 for the evidence), and the shipped user path is now a command rather than a script that opens the socket. |
| 2.9 | `PAUSED` handling and terminal market lifecycle | PARTIAL | ADR-0004. `packages/runner/src/strategy/lifecycle.ts` is the decision table: closed and resolved are TERMINAL, not-tradeable is PAUSE under the *original* expiry, and an unrecognized status is PAUSE rather than TERMINAL — pausing a job that should have ended costs it time it already had a bound on, ending one that could still have fired cannot be undone. It reads `status` and `tradeable` and nothing else; `tradeabilityReason` is an open set written for people, so matching it would make a server copy edit a control-flow change on every Runner (B4 would refine PAUSE, never detect it). `tests/strategy-lifecycle.test.ts` (10), exhaustive over the status × tradeable table. **`preflight` now calls it, and `driveJob` acts on the verdict**: every watching pass re-reads the trigger's market *and* each leg's, enters `PAUSED` on a PAUSE, leaves it for `WATCHING` — never straight into an order — when the market quotes again, and ends the job on a TERMINAL one. A pause writes no transition it already wrote, so a market closed for a day is one row rather than a log. `tests/strategy-driver.test.ts` covers enter, stay, leave, terminal and unrecognized-status. The gap: this happens only on a driven pass, so it happens on a tick in a daemon given a driver and not at all in one without (2.6). |
| 2.10 | Mandatory `expiresAt`, 7-day cap, no extension | PARTIAL | ADR-0005. Enforced at creation by `resolveExpiry` (`packages/runner/src/strategy/intent.ts`): absent is refused (`EXPIRY_REQUIRED`), a zoneless or locale-formatted instant is refused rather than parsed — `Date.parse('01/02/2026')` is the host's opinion, not the caller's — an instant at or before now is refused, and past the seven-day cap is refused with the latest instant that *would* have been allowed, **never silently clamped**, since a clamped expiry stops watching earlier than its owner asked. The accepted value is canonicalized to UTC and written once; nothing anywhere extends it, and pausing explicitly does not. 15 of `tests/strategy-intent.test.ts`'s cases cover this. `driveJob` now ends a job at its expiry on any pass that reaches it, before it reads a market or a quote, and a cancellation the owner asked for outranks it. `tests/scheduler.test.ts` proves the loop ends a job on the clock alone, with no market read. **The gap: a timer fires between start-ups only in a daemon that was given a driver** — in one without, a job that passes its expiry stays in its state until the next start-up recovers it (2.6). Nothing extends an expiry, and pausing explicitly does not. |
| 2.11 | Frozen-share percentage SELL; dynamic fraction as a distinct mode | PARTIAL | ADR-0001 §13, D-15. At creation: exactly one of `buyAmount`, `sellShares`, `sellFractionOfPosition`, `dynamicSellFractionOfPosition` may be given — none is `SIZE_MISSING`, more than one is `SIZE_AMBIGUOUS` listing what was given, and a size is never inferred from the side. `sellFractionOfPosition` is the **default** meaning of "sell half": it resolves against a real position read into a concrete `sellShares` on exact scaled-integer arithmetic (truncating toward zero, and refusing a fraction that resolves to nothing rather than submitting a zero-size order), and the record keeps `positionSharesAtCreation` and `frozenAt` beside it. The read proves absence rather than assuming it: it pages until the server answers `nextCursor: null`, and an unanswered cursor, a repeating cursor or a page bound is `POSITION_LOOKUP_INCONCLUSIVE`, not "you hold none". `dynamicSellFractionOfPosition` is the distinct explicit mode and performs **no** read at creation. `JobLegSizing` persists which mode a leg is in. **The dynamic mode now has its resolution point**: `preflight` re-reads the position at trigger for a `DYNAMIC_FRACTION` leg *only*, sizes off what is held then, and leaves a frozen leg untouched — a frozen job performs no position read at trigger at all. Absence is still proven rather than assumed: an inconclusive lookup **pauses the whole job**, while a *proven* absence, a market/outcome mismatch or a fraction that resolves to zero skips only that leg and leaves its siblings to trade. Covered by `tests/strategy-driver.test.ts`. The gap: as everywhere in 2.6, resolution happens only on a driven pass. |
| 2.12 | Restart / kill / network-partition tests | PARTIAL | The **restart** half exists at the store layer: `packages/runner/tests/recovery.test.ts` (23) stages each crash point against a real database, closes it, reopens it and asserts what the next Runner does — re-arm on the same key, `UNKNOWN_PENDING` on an unresolved attempt, reconcile on a recorded execution id, and hands off rather than racing when another instance holds the lease. These are in-process reopen tests, not a killed process: no `SIGKILL`, no partition, and no server on the other end. The daemon (2.6) adds start-up recovery and clean shutdown under test — `tests/daemon.test.ts` asserts that a seeded mid-write crash is recovered as `UNKNOWN_PENDING` and stays there, and that a second Runner refuses a live socket — but those daemons are started and stopped in-process. **The restart is now proved end to end through the shipped process**: a daemon with a driver arms and triggers a two-leg job, dies at the ledger row for the second leg, and a *second* daemon is started on the same reopened database with the same gateway — recovery hands it a `RECONCILING` job, its scheduler finishes the run, and the gateway counts exactly two creates for two legs under the keys minted before the crash; the ambiguous-create case runs the same way and counts exactly one create across both processes, with the job still visible in `UNKNOWN_PENDING` (`tests/daemon.test.ts`). **The thing that can now be interrupted mid-order exists, and is tested at every boundary**: `tests/strategy-driver.test.ts` injects the crash at the *store*, so the pass dies with exactly the rows a process killed one instruction earlier would have left, then closes the database, reopens it, runs recovery and counts what the gateway was ever asked to send. The five boundaries are: before the `CREATING` transition (nothing sent; the next pass re-arms and re-uses the **same persisted key**), during the create (one create ever, `UNKNOWN_PENDING` forever, no second order invented), after a recorded create and before the submit (recovery reconciles; zero submits), during the submit (one submit ever, resolved by *reading* the execution rather than resending it), and **between one leg's create and its sibling's** — the leg with no attempt row of any kind is provably one nothing was ever sent for, so the next pass either re-arms the whole job (nothing left the process; every key kept) or finishes the half-sent run under the key already on disk, after fresh authorization, market, position and quote checks. That resume cannot end the job: an order exists on the server, so a refusal, or the job's own `expiresAt` passing, marks the unsent legs `SKIPPED` and leaves the verdict to the read — asserted as one create before the crash and exactly one after it, under the pre-crash key, and as zero creates once the expiry has passed. **The `SIGKILL` half is no longer hypothetical.** On 2026-08-20 a configured `runnerd` holding a `WATCHING` job was killed with `SIGKILL` — no handler, no clean stop — and restarted against a real backend and a real chain: a different `instanceId` answered for the same job, still non-terminal, reporting `openSideEffects: []` rather than assuming it. That was one run, by hand, with nothing in flight at the moment of the kill, so it proves recovery of a watching job and NOT recovery of one killed mid-create or mid-submit — which remains injected in-process. **Still not covered: a network partition, and a kill timed into a write against a real server.** WP11 adds the *instrument* for the missing half but not the evidence: `strategy-restart-recovery` in the E2E plan restarts the Runner with the **operator's own command** (this repository constructs none) and then requires the same `jobId` to be answered by a **different** `runner.instanceId`, still non-terminal, with `openSideEffects` present — so a restart command that did nothing fails the step rather than passing it. It has never run: the `runner` and `runnerRestart` gaps are both MISSING here. |
| 2.13 | Notification sink (webhook + durable event log) | TODO | D-23 |
| 2.14 | Runner drain: stop admitting, finish what is in flight | DONE | ADR-0009. `packages/runner/src/drain.ts` + `runner.drain`; `packages/runner/tests/drain.test.ts` (11). Admission closes at **both** doors from one object, so they cannot disagree: `strategy.create` over the socket is refused `RUNNER_DRAINING` **and writes no job** — the test asserts the store is still empty, because a caller told its strategy was not armed must not find one armed later — and `JobScheduler` consults the same predicate before it claims, so a job appearing mid-drain is left unleased for the next Runner and no store read is paid for. Held jobs keep getting passes; that is the whole difference from `shutdown`. **What it waits for is the interesting decision.** Not "no job is non-terminal": a `WATCHING` strategy is non-terminal for up to seven days, so that definition would make every drain end by crossing its deadline and teach an operator to ignore the one signal that meant something. ADR-0009 says "terminal *or safely resumable*", and the store already records the difference — a watching job is resumable now, a job with an **open side-effect attempt** is a request that may have reached the server with nobody having seen the answer. So open attempts **started by this instance** are what a drain waits on, and `inherited` ones are reported but never waited on, because staying alive cannot settle them and the create-phase case is unresolvable against this API at all (B9). A deadline that passes is reported (`deadlineExceeded`, with `settling` naming each attempt, leg and kind), never crossed in silence. Drain **does not exit** — that would stop the process mid-write on exactly the runs where something was still settling — and there is no reopening: `runner.shutdown` is step two, and `docs/RELEASE.md` documents the pair. `runner.status` reports `draining` so a client learns it without calling `drain` and closing admission as a side effect of asking. A drain drives the loop itself rather than waiting out the tick interval, which is safe because `JobScheduler.tick` joins a pass in flight instead of doubling it. |

**Phase 2 exit criteria**: across a two-hour strategy with forced token expiry, a
WS gap, and a Runner restart, at most one logical execution results and every leg
reconciles independently.

## 5. Phase 3 — adapters, beta, operations

| # | Item | Status |
| --- | --- | --- |
| 3.1 | Host-neutral agent instructions | DONE — `packages/adapters/src/instructions.ts`, emitted to `agent-instructions/AGENT_INSTRUCTIONS.md` and compared byte-for-byte by `packages/adapters/tests/instructions.test.ts` (7), the same way `schemas/` is. Thirty rules in nine sections, each with a symbolic id (`SIZE_AMBIGUITY_STOPS_BEFORE_A_WRITE`, `RETRY_REPLAYS_THE_SAME_KEY_AND_BYTES`), so a deleted rule fails a test rather than shortening a document and a refusal can cite one by name. The command table in it is generated from the contract, so a host reads the same twenty-four commands, CLI paths and classifications every other surface does. The MCP adapter returns this document verbatim in `InitializeResult.instructions`; a host that cannot run this toolchain reads the committed file. **It is also emitted into `packages/sdk/AGENT_INSTRUCTIONS.md` and listed in that package's `files`**, and the command contract is emitted into `packages/schema/agent-commands.json` and listed in its own — because `npm install` delivers a tarball and nothing else, and both documents previously existed only at the repository root, which is to say only for readers who already had the source. `tests/workspace.test.ts` asserts each `files` entry and compares the shipped bytes to the committed copy, so a tarball cannot ship a snapshot of what the rules used to say. The two rules that arrived with it are the ones an SDK-only reader needed and the document did not have: `IDENTIFY_YOUR_SURFACE_BEFORE_THE_FIRST_COMMAND` (the opening rule told every reader to call `runtime.describe`, which is the one thing a library holder cannot do) and `A_SURFACE_YOU_LACK_IS_NOT_A_CAPABILITY_TO_REBUILD`. The command table gained an `SDK` column derived from the contract's own `implementation`, so a command the library cannot honour alone says `no — local Runner` or `no — composed by the core` rather than leaving a cell a reader fills in with an assumption. It states the decisions that cost money — a BUY target is the highest executable ask and a SELL target the lowest executable bid, `buyAmount` and `sellShares` are different units with no conversion, decimals are strings, a timeout `may already have executed` and is recovered by reading rather than by a fresh key, `driving: false` means nothing is watching, expiry is mandatory and capped at seven days, `execute-many` is `never atomic`, and an approval token `is not a credential`. |
| 3.2 | MCP adapter + one other function-calling adapter, same schema | DONE — `packages/adapters` (the shared core, 38 tests) and `packages/mcp` (the optional stdio transport, 30 tests). One registry projects the contract's twenty-four commands into OpenAI, Anthropic and MCP tool shapes; only the wrapper differs, and `tools.test.ts` fails if any host's schema drifts from another's. Each tool carries the transitive `$defs` closure it reaches and no more, so no host receives a dangling `$ref` or twenty-three definitions to list a market. MCP annotations are derived, not listed: `readOnlyHint`/`idempotentHint` from the command's classification, `destructiveHint` from `MOVES_FUNDS`, and `openWorldHint` from the contract's own `Local only:` marker. **Delegation is structural**: an adapter spawns the installed `waterx-predict` binary and never imports the SDK or the Runner — `packages/adapters/tsconfig.json` resolves only the schema, and `tests/workspace.test.ts` fails if either adapter's source names the SDK, the Runner or the CLI as an import — so there is no symbol in scope to reimplement pricing, retry, signing, policy or job state with. The dispatcher validates with the CLI's own validator, forwards one JSON document, relays the envelope verbatim, and never retries. Approval cannot be granted through an adapter: `--approve` digests one exact intent and is refused from the operator-flag allowlist, so a write under the default `interactive` policy is refused `POLICY_DENIED` by the core. `isFullySettled` requires `ok` **and** exit 0, so MCP reports a partially filled `execute-many` with `isError: true` rather than as an unqualified success. `tests/workspace.test.ts` runs one intent through both front doors and asserts identical argv. **What this does not mean:** no adapter has been driven by a real model host, and no OpenAPI document is emitted — this repository serves no HTTP surface, so one would describe an endpoint that does not exist (an adapter that wanted it would build it over the same registry). |
| 3.3 | Server capability advertisement consumed by `describe` | BLOCKED (backend) |
| 3.4 | Quote-to-fill deviation, WS latency, reject-reason dashboards | PARTIAL (backend) — the quote stream exports freshness (`value_age_seconds`), the WaterX-controlled portion (`delivery_lag_seconds`), frame counts by kind, subscription/stale-topic coverage and rejections by reason (backend `2aedb8d8`); the execution path now adds quote-to-fill deviation, terminal outcomes, reject reasons and revocation-detection latency (backend `5ab9d9e4`, `predict-agent-execution.metrics.ts` + 10 tests, deviation arithmetic in `domain/quote-deviation.ts` + 12). Deviation is signed so that **positive is always worse for the agent** on both sides, and is `null` rather than `0` when the share count or the quote price was not observed — a zero would report a perfect fill where nothing was measurable, and the unmeasurable population is counted separately (`…_unobservable_total` by reason). Revocation latency is published as an **upper bound**: it measures from this process's last allowed write, not from the on-chain revocation, which nothing here observes; a mandate this replica never saw publishes nothing rather than a fabricated zero. The private execution socket — the one carrying fills, and until now the only agent surface exporting nothing — is covered by backend `f14d2a5f` (`predict-agent-stream.metrics.ts` + 6 gateway tests): handshakes by result, replayed frame depth, handshake rejections by reason, and authenticated connections on the pod. A gap is a **label on the handshake counter, not its own series**, so the gap rate is a division inside one series (`GAP / (RESUME + GAP)`) and can distinguish a replay window that is too small from one that is merely well used; `FRESH` is outside that denominator because a client with no cursor asked for no history and could not have missed any. The connection gauge is incremented only after a wallet is established and decremented only in that same branch, so a pod refusing every token cannot drive it negative. **What this does not mean:** these count the socket, not the delivery — the durable record is the outbox and the authoritative one is a REST read, so a quiet `GAP` series proves only that nobody on that pod asked from too far back. And no dashboard and no alert rule is committed — this repository does not own one, and the backend `2aedb8d8`/`5ab9d9e4`/`f14d2a5f` series are Prometheus exports that an owner still has to graph and threshold. |
| 3.5 | Thin-market large-size and high-subscription load tests | TODO |
| 3.6 | npm provenance, SBOM, upgrade/rollback docs, beta support policy | PARTIAL — **the policy and the artifacts exist; nothing has been published, and one gate is unresolved.** ADR-0009 decides D-26 through D-30. `docs/RELEASE.md` is the procedure: what ships, the pre-publish gates, provenance, the SBOM, the support window, upgrade/rollback, and telemetry (none, in any package). Provenance is requested where it cannot be lost by editing a workflow line — `publishConfig: { access: "public", provenance: true }` in each published manifest, asserted by `tests/workspace.test.ts` — and `.github/workflows/release.yml` is `workflow_dispatch`-only with `dry-run` defaulting to `true`, `id-token: write`, and a publish step gated on `dry-run == false`. The SBOM is a committed CycloneDX 1.6 document per published package (`sbom/v1/`), generated by `packages/release` (54 tests) from the **installed** tree rather than from declared ranges, hashed from the lockfile, carrying no timestamp and a content-derived serial so `git diff --exit-code -- sbom/` in CI means something. Licences are never guessed: `xmlhttprequest-ssl@2.1.2` declares none (it uses npm's deprecated `licenses:` array and ships a verbatim MIT text), so a human read it and the finding is pinned to that exact version in `src/license-review.ts` with its evidence, emitted with `waterx:license-source: HUMAN_REVIEW`, and **failed** by the preflight the moment the version it covers stops shipping. `pnpm release:preflight` runs ten checks in three outcomes — PASS, FAIL, and UNRESOLVED for a fact that lives outside this workspace — and `--strict`, which the release workflow runs, refuses on UNRESOLVED. Current state on a built workspace: **9 PASS, 0 FAIL, 1 UNRESOLVED** (`dist-built` is the tenth and fails before `pnpm build`, which is why ordinary CI runs the preflight report-only). The unresolved one is `repository-provenance`: no git remote is configured here, so no `repository` URL is declared, and provenance has nothing to attest the build *to*. That is the blocking gap, and it is an infrastructure fact nobody in this repository can supply. **What this does not mean:** nothing has been published, no provenance attestation has ever been produced, the release workflow has never run, and the SBOM's completeness is only as good as the tree that was installed when it was generated. The **first wave is the SDK and the schema only** (D-28): `@waterx/predict-agent-cli`, `@waterx/predict-agent-adapters`, `@waterx/predict-agent-mcp` and the Runner stay `private` until 1.11 has actually run against a real server. The drain path (2.14) was the other half of that condition and now exists; 1.11 is what still holds them back, and it is the half no work in this repository can close. |
| 3.7 | The installed package answers for itself: shipped rules, a discovery entry point, a skill, and `doctor`'s requirement list | DONE — the four halves of "`npm install` succeeded, now what", which nothing published could answer. **(a) The documents ship.** `packages/sdk` lists `AGENT_INSTRUCTIONS.md` and `SKILL.md` in `files`, `packages/schema` lists `agent-commands.json`, all three are emitted by the existing generators into a second destination inside the package, and `tests/workspace.test.ts` compares the shipped bytes to the committed copy so a tarball cannot carry a snapshot of what the rules used to say. Verified by packing and installing, not by reading `files` — and that verification is now a tool rather than a one-off: `packages/release/src/consumer.ts` backs `pnpm consumer:kit`, which writes a portable project whose `file:` dependencies are the packed tarballs, and `pnpm consumer:registry`, which serves the same tarballs scoped over HTTP so `npm install <name>` resolves by name. Both pack whatever `publishedPackages` reports, so the `private` CLI cannot be served by either; the registry's routing is a pure function so the suite still opens no socket. `pnpm consumer:check` runs the same install to a verdict and is a CI job (`install` in `verify.yml`): it asserts every literal `files` entry, `main`, `types` and every `bin` arrived, imports each package by name from the install, and spawns each binary far enough to prove it resolves its own imports. Its expectations are derived from `files`, which cuts one way — it catches `files` promising what did not arrive, and cannot catch `files` forgetting to promise something, because the expectation disappears with the entry. That half is `tests/workspace.test.ts` requiring the agent-facing documents to be listed at all; neither is sufficient alone. **(b) Discovery without configuration.** `packages/sdk/src/installation.ts` + `provisioning.ts` (13 tests) declare the seven requirements a trade needs, who supplies each, and how each is settled; `describeInstallation()` resolves the four an environment can settle and reports the three behind an authenticated read as `UNCHECKED`, never `MISSING`. `bin: waterx-predict-agent-sdk` makes it `npx`-able, and `tests/workspace.test.ts` fails if that entry point ever imports the client, the transport, a session, `fetch` or a subprocess — a discovery command that could place an order would be an order with none of the core's policy, approval or idempotency (`NO_SECOND_SURFACE`). This is **not** the CLI's `describe`: it is what the only publishable package can honestly say while D-28 keeps the CLI private. **(c) A skill that routes rather than restates.** `packages/adapters/src/skill.ts` (8 tests) emits `SKILL.md` from the same generator as the instructions: frontmatter trigger, seven steps, and a hard-stops table whose every entry is a rule id asserted to exist in `instructions.ts`, so deleting a rule breaks the skill instead of leaving it pointing at nothing. A test fails if it grows past half the instructions' length, because a skill that restates the rules is a second copy and the one a host loaded is the one nobody re-reads. It states where the flow stops — a write through a tool adapter is refused `POLICY_DENIED` by design, so the completed task is a previewed order and the line a person runs. **(d) `doctor` says what to do, not only what happened.** `packages/cli/src/requirements.ts` settles the same seven from a configured CLI, adding the three an authenticated `listAuthorizedAccounts` reaches, and `runDoctor` reports `requirements` / `missing` / `unchecked` / `nextStep` alongside its checks (13 tests in `doctor.test.ts`). An owner's outstanding grant is deliberately **not** a failing `doctor`: the exit code keeps meaning "your setup is wrong" and `nextStep` says "your owner has not signed yet". The preflight's `dist-built` now covers `bin` as well as `main`/`types` (3 tests), since a `bin` typo is `command not found` for every consumer and for nobody in this workspace. **What this does not mean:** nothing is published, so none of it has reached a consumer through a registry — the install was verified from a local `npm pack` tarball. No model host has been driven through the skill. And the CLI is still `private`, so `npx @waterx/predict-agent-cli describe` — the canonical discovery path — remains blocked on D-28 and 1.11, which this item does not move. |
| 3.8 | `runtime.next` — the loop an agent host obeys | DONE — `packages/cli/src/commands/next.ts` (`packages/cli/tests/next.test.ts`, 33). One read-only command that answers "where does this agent stand, and what now" in every state, including before anything is configured, with exit 0 whenever it answered. Ten states, checked worst-first by the pure `decideNext`: `SETUP_INCOMPLETE`, `SESSION_FAILED`, `AUTHORIZATION_UNKNOWN`, `AWAITING_OWNER`, `ACCOUNT_CHOICE_NEEDED`, `ACCOUNT_UNREADABLE`, `UNSETTLED_EXECUTION`, `STRATEGY_NEEDS_ATTENTION`, `TRADING_BLOCKED`, `READY`. The order is the safety property and is pinned by precedence tests that set every later condition at once: a non-terminal execution (newest 50 scanned) or a Runner job in `UNKNOWN_PENDING`/`RECONCILING` outranks the limits and every trade suggestion, and an account whose limits, executions or positions could not ALL be read is `ACCOUNT_UNREADABLE`, never `READY`. **What makes "do what it says" bounded:** every suggestion names a contract command, is asserted to be a `read` (a write throws at construction), and carries `input`, `argv` and `invocation`; the suite runs every complete suggestion back through the CLI's own parser and requires it not to be refused as malformed — which is how it caught that `order preview`, a `oneOf`, takes `--input` rather than typed flags. A person's step is data: `stop`, `actor` and `handOver` (who, what to tell them, the authorization link, the operator settings), and values a person must choose — the account, the market, the side, the size, the slippage bound — come back as `needsFromUser` and are never filled in. A failed listing or a null chain read is `AUTHORIZATION_UNKNOWN`, not `AWAITING_OWNER`. The Runner is glanced at with one `strategy.list` and only when its runtime directory is there; no Runner is `ABSENT` with zero dials. Composes the same reads `doctor` and `onboard` do; its only signature is the login challenge. Rule `NEXT_IS_A_ROUTE_NOT_A_MANDATE` (instructions v4) and a skill step (skill v2) tell a host how to loop on it. **What this does not mean:** no model host has been driven through the loop, and no state past `SETUP_INCOMPLETE` has been answered by a real server — the install check below reaches only that one. |
| 3.9 | The operator bundle: the CLI as one installable tarball | PARTIAL — **built and verified; releasable since ADR-0010 was Accepted (2026-09-16), and not released.** `packages/release/src/bundle.ts` (`tests/bundle.test.ts`, 16) packs the CLI with pnpm (the only tool that rewrites `workspace:*`) and the SDK and schema with npm, places the latter inside it as `bundleDependencies`, lifts their third-party dependencies onto the bundle's own (two ranges for one name is refused), forces `private: true`, drops scripts, ships `sbom.cdx.json`, and refuses a tarball whose listing lacks the binary, `next`, the SBOM or either library, or carries sources, tests or source maps. `pnpm cli:bundle` writes the tarball, `SHA256SUMS` and the SBOM; `--release` reads ADR-0010's `Status` line and refuses unless it is `Accepted`. `pnpm cli:bundle:check` — a CI step in `verify.yml`'s `install` job — installs the tarball into a throwaway project with **npm alone and `--ignore-scripts`**, confirms the libraries were not also fetched beside it, and runs `npx --no waterx-predict next --json` and `describe` with an emptied environment and a throwaway `HOME`, requiring `SETUP_INCOMPLETE`, `stop: true` and exit 0. Run here on 2026-09-16: passed; the installed tree held `socket.io-client` and its chain at the top level and both libraries only inside the CLI. The bundle SBOM is committed at `sbom/bundle/` and compared byte-for-byte (`tests/artifact.test.ts`). `release.yml` gains a `cli-bundle` job — manually requested, never on a dry run, after every `release` gate, `contents: write` scoped to that job only — that attaches the three files to release `v<version>` via the gated command. **What this does not mean:** nothing has been attached to a release, the release workflow has never run, a private repository's asset needs an authenticated download, and the checksum identifies the released file rather than a reproducible build. |
| 3.10 | The CLI defaults to mainnet | DONE — ADR-0011. `packages/cli/src/config.ts`: nothing named resolves to `production` (`deploymentSource: 'DEFAULT'`), `mainnet` is an alias for it, and an unknown name is NOT defaulted (`'NONE'`). Every envelope carries the real-funds warning (`envelope.test.ts`); `describe` reports `api.deploymentSource`, `doctor`'s `config` check and the `deployment` requirement state it (`doctor.test.ts`), and `next` reports `facts.deployment` with `realFunds` and no longer asks for the network (`next.test.ts`). `packages/e2e/src/preflight.ts` refuses to count a defaulted host as a supplied base URL, so the harness never reads against mainnet by omission (`harness.test.ts`, including the existing bare-environment run through the real binary). The SDK client still requires a deployment. **What this does not mean:** no request has been sent to mainnet by any of this work; every test stubs the transport and the bundle check runs with no signer. |
| 3.11 | The operator can get a signer: the keystore ships beside the CLI | PARTIAL — **built and walked; releasable since ADR-0012 was Accepted (2026-09-16), and not released.** `packages/release/src/bundle.ts` now builds two operator artifacts from `OPERATOR_ARTIFACTS` — the CLI bundle and `waterx-predict-agent-signer-keystore-<v>.tgz` (`files` added to its manifest) — refuses artifacts of different versions, writes one `SHA256SUMS`, and commits both SBOMs under `sbom/bundle/` (the keystore's: 20 components, all licensed and hashed; `@mysten/sui` is in it and asserted absent from the CLI's). `--release` now needs ADR-0010 **and** ADR-0012 Accepted (`tests/bundle.test.ts`). `next` gained `src/keystore-probe.ts`: PATH lookup through a new optional `CliIo.findExecutable` seam, the public `address` from `keystore.json`, and whether the agent socket exists — no import, no dial, and a layout copy held equal to the keystore's `AGENT_PROTOCOL` (`tests/workspace.test.ts`). From it `handOver.steps` lists only the undone steps as commands (install, `init`, `agent`, the wallet with the address filled in, the signer command); a keystore step left undone, or an agent wallet the keystore does not hold, stops `next` before any signature; a signer failure with the keystore configured sends the operator back to `agent` (`packages/cli/tests/next.test.ts`, 9 cases). `pnpm cli:bundle:check` now installs both tarballs with scripts off and **walks** it: `next` on a bare machine names `init` first; `init` creates a wallet under a passphrase file; `next` fills that address in; `agent` starts; `next` configured against a stub on 127.0.0.1 answers AWAITING_OWNER with a link naming the address; the stub grants and `next` answers READY with market search and order preview; and the login signature the stub received is verified with the installed Sui SDK against the keystore's address. Run here on 2026-09-16: passed, no agent process or keystore directory left behind. **What this does not mean:** the API was a stub, nothing reached a real server, and no model host drove the loop. |
| 3.12 | The agent API answers on mainnet | BLOCKED (infra) — **it does not: deployed, and switched off.** One unauthenticated `GET https://api.waterx.app/agent-api/v1/predict/markets`, authorized by the user on 2026-09-16, answered `HTTP 500` `{"code":"RECONCILIATION_REQUIRED","message":"The Predict Agent API is not configured on this deployment"}` — the route exists and the guard refuses because no signing secret is set. Until an operator sets it, a CLI left on its mainnet default (ADR-0011) stops at `SESSION_FAILED` with that sentence. The repository reading that led here, 2026-09-16: the agent API is on `bucket-backend-mono` `origin/main` (#1188, #1235) and is imported unconditionally into the waterx app (`predict.module.ts:80-81`), but it is inert until `PREDICT_AGENT_API_JWT_SECRET` is set (`agent-api.module.ts:6-9`, `config.schema.ts:322-324`), and that secret lives in the external k8s-infra production overlay or the GCP secret store, not in the backend repo. Production deploys are a manual `deploy-gcp.yml` run from `main`; nothing in the repo shows whether one ran after #1235. The console's `/agent/authorize` page is on `waterx-fe` `origin/main` (the local checkouts are stale). **A finding for the backend:** an unconfigured deployment refuses the login and every guarded route with `RECONCILIATION_REQUIRED` (`predict-agent-auth.controller.ts:46`, `predict-agent-api.guard.ts:36`), a code that means an execution needs reading back; it deserves its own code, which is a wire-contract change made backend-first. Until then `next` relays the server's message verbatim beside the code rather than reinterpreting it (`next.test.ts`). Apart from that one read, nothing here made a request to production. **To close it:** set `PREDICT_AGENT_API_JWT_SECRET` in the production overlay (an infra action), and give the not-configured refusal its own code and a 503 (a backend contract change). |
| 3.13 | A model host has walked the loop | PARTIAL — **walked once, by a model, against a local stub; never against a real server.** 2026-09-16: Claude, acting as the agent host, installed the two operator tarballs with `npm install --ignore-scripts` into a scratch project and followed `npx --no waterx-predict next --json`. Bare machine → `SETUP_INCOMPLETE`, `stop: true`, four operator steps (`keystore init`, `agent`, the two settings) and the mainnet-default warning; the host stopped and handed over. The operator steps were then run (clearly as the operator, not the agent), with the network pointed at a stub on 127.0.0.1 because development may not reach mainnet → `AWAITING_OWNER` with the owner's link; the host ran the suggested `onboard --wait` while the stub granted → `READY`; the host asked the user, ran `market search` (RESOLVED) and `order preview` (priced, `placed: false`, approval token issued); and an `order execute` without `--approve` was refused `POLICY_DENIED`, exit 5, with **zero** requests reaching the stub. **One defect found and fixed:** `buildAuthorizationUrl` resolved the absolute `/agent/authorize` against the console URL and dropped any path prefix, sending an owner of a console served at `https://host/console` to a page that grants nothing (`packages/sdk/src/onboarding.ts`, test in `onboarding.test.ts`). **What this does not mean:** the API was a stub, the owner's grant was a canned response, the host was one model in one session, and no order was placed anywhere. **Walked by a model host from the merged main, 2026-09-18** (session `9bc93ea4`, a separate Claude Code in a clean project): it was given the sentence only, ran `npm install github:WaterXProtocol/waterx-predict-agent` (34 s, 31 packages) and `npx --no waterx-predict next --json`, read `agent-instructions/`, and then **stopped at the hand-over and relayed it** — it did not create a wallet, export a setting or trade, and it passed on the mainnet/real-funds warning. Four Bash calls and two reads, no flailing. Two gaps it exposed, fixed the same day: the hand-over omitted `keystore agent` when a stale socket file was present (a month old, no agent behind it), and npm's `allowScripts` warning about the root `prepare` was undocumented. **Still unwalked:** everything after the hand-over — the operator's steps, the owner's grant, READY, and a trade. |
| 3.14 | The CLI trades without the Agent API (direct mode) | PARTIAL — **built and tested offline; never run against a real server, and no order placed.** ADR-0013 (Accepted 2026-09-17). `packages/sdk/src/sui-tx.ts`: a dependency-free BCS decoder for `TransactionData` V1, held byte-for-byte to `@mysten/sui` 2.26.2 (`tests/sui-tx.test.ts`). `packages/sdk/src/direct/`: `PredictDirectClient` over the public routes (`/predict/browse`, `/predict/quotes*`, `/predict/bets/place|sell`, `/sponsor/execute`, `/predict/bets/me*`, `/account*`), the deployment document from `config.waterx.app` (5-minute cache, a failed refresh fails), a verifier that refuses any sponsored transaction that is not exactly the intent (sender, gas owner, command kinds, package ids, shared-object roles, every argument, the sell floor; `tests/direct-verify.test.ts`, bytes from `@waterx/sdk` 5.0.0 — the builder the backend uses), price protection ported from the Agent API, an intent journal that records the digest before submitting (a caller's key is bound into the record), settlement from the owner's activity feed and Sui GraphQL, and `listUnsettled` from the journal (`tests/direct-client.test.ts`, 26 cases). `packages/cli`: `WATERX_PREDICT_MODE` (default `direct`) and `WATERX_PREDICT_NETWORK`; `TRANSACTION_REFUSED` (exit 10) and `DEPLOYMENT_UNAVAILABLE` (exit 7); `doctor` checks the deployment document instead of logging in; `order preview` and `account status` report `DIRECT_MODE_NO_SERVER_MANDATE`; a delegated-auto BUY skips the server capacity read; `next` reads unsettled orders from the journal (`~/.waterx-predict`, `0600`); `describe` and `onboard` say which mode runs (`tests/direct.test.ts`, 10 cases through the real dispatcher). `pnpm cli:bundle:check` now also walks direct mode to READY against the stub with zero `/agent-api` requests. **Walked live on testnet, 2026-09-17** (built CLI + keystore signer, `api-testnet.waterx.app`, zero `/agent-api` requests): catalog, search, market, quotes (equal to the public boards); `next` READY on an owner-signed delegation (tx `EefE4NUU…`, predict mask 9) read from the chain; `order preview` → approval → `order execute`: the backend's sponsored bytes — deposit 500 MOCK_USDC, mint wxUSD, consume, `place_order` — passed the verifier, the keystore signed, the sponsor executed (`BCG6A3ZX…`, SUCCESS, `OrderPlaced` 1857); the unfunded first attempt was refused by the sponsor's dry run (`CHAIN_REJECTED`, exit 10) and left no journal record; the wait timed out as `SUBMITTED` (exit 11); `order reconcile` and `next` (`UNSETTLED_EXECUTION`) held on it; a replay under the same key read it back and sent nothing. Found and fixed on the way: null catalog titles; `riskProfile` reported missing in direct mode; `onboard --wait` bounded by the 15 s request deadline (both modes); a poll that gave up on the first 429 and polled 4× faster than `/account/delegated` allows (300/h) — now 15 s, `retry-after` honoured, 429 not retried inside the window; the testnet delegation index not listing a grant 75 min old — a named account is now verified from the on-chain `Account` object (type and permission key matched to the deployment's original ids). **Not reached:** a fill. The testnet keeper did not fill or cancel order 1857 and the activity index did not list it, so settlement (read from that index) stays `SUBMITTED`; SELL was not exercised. Settlement has no chain-side fallback: a fill does not touch the account object, so finding one needs an index. **A settled BUY could read as pending for ever, and that is fixed.** The registry's `position_id_by_order` was the only reader that could prove a buy filled, and `remove_and_drop_position` drops it the moment the position is sold or claimed (`waterx_prediction.move`, from `close_confirmed` and `position_claimed`) — after which the chain answers `GONE`, the same answer it gives a cancel. The feed cannot break the tie either: a `bought` row is position-backed, so the backend keeps order ids off it by design (`bet/domain/activity.ts`) and stamps it with the keeper's digest, leaving nothing on it that points back at the submission. The tests said otherwise only because their fixtures put an order id on a `bought` row, which no server sends; those fixtures now carry the server's shape. So `readExecution` asks a third reader last — the indexed bet history (`/predict/bets/me?filter=all`, keyed on the order id the chain or the feed named), whose row outlives the position — and a fill is joined to the feed by POSITION rather than by order. Both histories are now walked rather than read once, bounded by pages and by the order's own placement time, since one page of a hundred rows was an assumption that the account had not traded since. Nothing found still settles nothing: a failed or exhausted read leaves the order non-terminal (`tests/direct-client.test.ts`: sold-position settle, fill absent but status known, unfilled vs still-pending, the paged walk and its bound). **Not available in this mode:** allowance, risk limits, execution history, fills, performance (`CAPABILITY_UNAVAILABLE`); positions of a non-main account. |
| 3.15 | Direct mode is as defended as the perp agent | DONE — ADR-0014/0015/0017, 2026-09-17. **Approvals** are issued by `order preview`, expire in ten minutes and are spent before signing (`packages/cli/src/ledgers.ts`, `tests/ledgers.test.ts`, `tests/write-plane.test.ts`). **`maxCumulativeBuyAmount`** is counted per scope across invocations and released only when nothing was signed. **The digest** is computed from the bytes (BLAKE2b per RFC 7693, `packages/sdk/src/sui-digest.ts`, held to `@mysten/sui` and Node). **Settlement** reads the registry first (`orders`, `position_id_by_order`, `positions` through Sui GraphQL) — `EXPIRED` once `expiry_ts + 300 s` passes, with the held escrow reported under `refund`. **Contract shapes** are pinned (`packages/sdk/src/direct/abi.ts`) and checked before the first signature, in `doctor`, by `pnpm --filter @waterx/predict-agent-sdk abi:check` and daily in `.github/workflows/abi.yml`. **Discovery** falls back to the chain's grant events when the index lists nothing; a named account is verified from its object. **Adoption**: `next` never moves to a different account unless it is named. **`doctor --probeWrite`** builds and verifies a 1 wxUSD order without signing. **Mainnet is read-only** until the operator sets a policy. Live on testnet the same day: `contract-shapes` PASS on both networks, an unlisted grant found from events, `--probeWrite` PASS, order 1857 read as `EXPIRED` with its escrow and cancel window. |
| 3.16 | The Runner trades in direct mode | PARTIAL — **built and tested; no strategy has run against a live keeper.** ADR-0016. `packages/runner/src/config.ts` (`mode`, `network`, gap `network`), `runtime.ts` (a `PredictDirectClient` gateway that signs nothing; `createExecution` / `submitExecution` / `getDelegation` in the SDK), `strategy/preflight.ts` (`mandate: 'NONE'` checks the delegation only), `prices.ts` (`PollingPriceObserver`), the market's live round phase from its page, and `FileMarketCatalog` shared with the CLI (`tests/direct-mode.test.ts`, `tests/config.test.ts`). The testnet keeper did not fill orders on 2026-09-17, so a fill is covered by tests only. |
| 3.17 | Owner-side and backend pieces direct mode still needs | BLOCKED (other repositories) — **not in this repository, and not changed from it.** (1) `bucket-backend-mono`: a public build route for `self_cancel_order` / `self_cancel_close`, so an expired order's escrow can be recovered by the agent (today only the owner, in the app). (2) `waterx-fe` `/agent/authorize`: grant the cancel bit (2) with the others, and a direct-mode variant that does not write an Agent API mandate after the signature (today that second step fails on mainnet while the grant has already landed). (3) Testnet: the keeper did not fill or cancel new orders, and the delegation and bets indexes did not list new events, on 2026-09-17. (4) `npm install github:…` stays unsupported: the repository root is a private workspace (ADR-0010); the operator bundle is the install path. |
| 3.18 | Approvals and write decisions are auditable | DONE — ADR-0018, 2026-09-17. `--approve` requires `--approver <name>` (`packages/cli/src/run.ts`), recorded on the spent approval and in `write-audit.jsonl` — append-only, fsync'd, `0600`, never pruned: `approval.issued`, `approval.spent`, `spend.reserved` / `spend.released`, `account.adopted` (basis, `namedBy`, previous account), `write.result` (executions or the refusal). `doctor --probeWrite` also probes a close of a held position. Deployment-document and price freshness need no new code: the document is never used past five minutes, and the backend's public quotes are aged out server-side (`predictQuoteTtlMs`) to `null`, which already refuses an order (`tests/write-plane.test.ts`, `tests/ledgers.test.ts`, `tests/direct.test.ts`). |
| 3.19 | The repository installs with npm, as one sentence | DONE — ADR-0019, 2026-09-18. `npm install github:WaterXProtocol/waterx-predict-agent` installs both binaries: the root gained `version`, `bin`, `files` and the real third-party dependencies; `scripts/prepare-git-install.mjs` runs only for npm (a developer's `pnpm install` returns immediately), picks pnpm from PATH / corepack / `npx pnpm@<pinned>`, installs, builds and assembles; `packages/release/src/install-tree.ts` copies the four built packages and rewrites the only two cross-package specifiers, refusing a missing build, a leftover `@waterx/…` import or a missing binary entry. `pnpm install:check` (`packages/release/src/bin/install-check.ts`, in CI) packs the tree the way a git install packs it, installs it with scripts off and walks `describe` → `next` → `keystore init` → `next`. Walked here on 2026-09-18: 260 KB tarball, 81 files, all four steps passed. **What it does not prove:** that GitHub serves it — nothing clones over the network, and a private repository still needs the caller's git credentials. |
| 3.20 | An agent host can finish its own setup | DONE (code + local walks) — ADR-0020, 2026-09-18. Two model-host sessions (`9bc93ea4`, `be2c7314`) ran the one-sentence install and stopped at a hand-over whose three steps no tool host can perform: a resident `keystore agent` that blocks and wants a typed passphrase, and two `export`s that die with the child process that ran them. Fixed in three parts. **(1) A passphrase-less keystore** (`packages/signer-keystore`): `init --no-passphrase` writes `protection: "NONE"` — the raw key, base64, `0600`, with the warning inside the file — and `sign` opens it directly, so no agent, socket or token is involved; `agent` refuses a plaintext keystore, `openKeystore` reads both shapes, and files written before the variant existed are still sealed. `agent --detach` starts the sealed holder in the background, reading the passphrase in the process that has a terminal and handing it to the child on a pipe (never `ps`, never a file), then waiting for the socket to answer. **(2) `waterx-predict configure`** (`packages/cli/src/commands/configure.ts`, `runtime.configure` in the contract): the only command that writes settings, and it writes exactly two — `agentWallet` and `signerCommand` — `0600`, keeping an existing value unless `--replace`, and reporting when the environment will shadow what it wrote. It writes no network, policy or account (ADR-0017 stays the operator's), and it is a `write` in the contract that still moves nothing. **(3) `next` splits its steps**: `handOver.steps` for the person, a new `agentSteps` for what the host may run itself, each with a `safeBecause`; when every remaining local step is the agent's the answer no longer stops. `pnpm install:check` now takes the commands out of `agentSteps` and runs them rather than spelling them — from a packed git install to a configured wallet with no person and no resident agent — and `pnpm cli:bundle:check` still walks the sealed path with a resident agent to READY. **Not proven:** no model host has walked the new shape yet, and nothing here was run against mainnet. |
| 3.21 | The install and the policy both end somewhere | DONE (code + local walks) — ADR-0021, 2026-09-21. Two paths a real installer walks that ended nowhere, both found first in the perp agent's install sessions (`waterx-agent` #25, #28, #30) and both present here unchanged. **(1) An install with no build in it.** Measured on npm 11.16.0 against a packed install with scripts off: `ERR_MODULE_NOT_FOUND`, empty stdout, exit 1 — the code this contract reserves for USAGE — on the very first command, for both binaries. `bin/missing-build.mjs` (imports nothing but Node, because everything else is what is missing) now reports `BUILD_MISSING`, exit 3, the human text on stderr and, under `--json`, one envelope on stdout. The remedies in it are the MEASURED ones: re-running the git install works; `npm rebuild` does not (it reports success and runs no `prepare`); re-installing a packed tarball of this repository does not either (npm runs `prepare` for a git dependency and a directory, not for a tarball). Held by four tests in `tests/workspace.test.ts` that run the shims against a layout with no `dist/`. **(2) A policy nothing could set.** ADR-0017's opt-in lived in `WATERX_PREDICT_POLICY`, `next` said `export WATERX_PREDICT_POLICY=interactive` on its READY answer — the advice ADR-0020 removed everywhere else — and `configure` refuses to write a policy by design, so nothing in this package could turn writes on and the documented loop ended one step short of a trade. `runtime.policy` is a chooser (a READ, so `next` suggests it in the read-only posture) returning all three modes in rank order with what each means, what it costs, and the command that takes it as its own unwrapped field; `runtime.policy-set` writes `policy.mode` and leaves `policy.scope` alone. Narrowing needs nothing, widening needs `--yes` (a dispatcher flag, unreachable from `--input <json>`, like `--approve`), and `delegated-auto` is refused until a scope exists — named where the option is offered, not at the refusal. `ExecutionPolicy` gained `hasConfiguredScope`, separate from `scope`. The `export` advice is gone from `meta.warnings` and the read-only refusal's `remedy`, which are the surfaces an agent reads on every answer. Walked locally end to end: mainnet default read-only → chooser → refusal without `--yes` → `--yes` → config file → `describe` reporting `source: CONFIG_FILE, writesAllowed: true`, with no order placed. |
| 3.22 | No answer is a dead end, and each step says whose it is | DONE (code + local walks) — ADR-0022, 2026-09-21. Two shapes from the perp agent's install sessions (`waterx-agent` #31, #32), both present here. **(1) Outcomes with no exit.** Only `next` and `configure` handed a pointer back; `onboard` returned prose (`nextStep.action`) with no command in it, and every refusal ended with an error and nothing else. `meta.nextCommand` is now on EVERY envelope, success or refusal, defaulting to `waterx-predict next` (which answers in every state). `context.pointTo` lets a command name a better one and REFUSES two kinds — a `<placeholder>`, because filling in a value a person must choose is the one thing a host must not do (the `order execute --approve <token>` line is deliberately not a pointer), and `--yes`, a person's consent to widen the policy (ADR-0021). `meta` is therefore never absent; the two tests asserting it could be were asserting that an answer could be terminal. **(2) A step attributed to the wrong person.** `next`'s `AWAITING_OWNER` keeps `stop: true` and `actor: ACCOUNT_OWNER` — the signature really is the owner's — and now also carries `agentSteps: ['waterx-predict onboard --wait']` with the argument for why it is the agent's, and the headline says to run it while they sign. In the perp session that shape was the difference between handing over a link and stopping with nothing to hand over. `onboard` points at `onboard --wait` until the grant lands, including after a wait that ran out. Walked locally: bare → `keystore init --no-passphrase`; after configure → `next`; AWAITING_OWNER → `onboard --wait`; a refusal → `next`. |
| 3.23 | `next` says what the account is carrying | DONE (code + tests) — ADR-0023, 2026-09-21. `next` fetched the positions and the unsettled executions on every call — it needs them to pick a state — and then used them as booleans: positions became a count, unsettled became "is this list empty", and everything else was dropped before the answer was built. So `READY — Authorized on 0x… with nothing in flight` was reachable on an account holding positions in a market that had stopped quoting and a BUY submitted an hour earlier with its escrow still locked, both facts in memory when that sentence was written (the perp agent's #28, on a live mainnet account). `packages/cli/src/commands/exposure.ts` is a pure function of what the call already fetched, returning `DEPLOYED` (positions and cost — the number that needs no quote), `UNPRICED_POSITION` (no live sell-side quote: value and PnL unknown, NOT zero), `UNSETTLED_TOO_LONG` (past the keeper's five-minute grace, escrow held) and `BELOW_KEEPER_MINIMUM` (a BUY under 2 wxUSD will be cancelled rather than filled — measured on mainnet, order 38308, `below_min_fill`). Three rules hold it: no note costs a request; they ride on EVERY state and change none (`decideNext` decides first and attaches them to what comes back); and it refuses to measure what it cannot — cost is totalled, a portfolio value over a missing quote is not. Each note carries a `look` that is runnable as printed, and they print before the headline. `NextFacts.account.positions` is now the summaries rather than a count, and `NextFacts.now` is injected so the age of an order is measured against one instant. |
| 3.24 | The link reaches an owner who is somewhere else | DONE (code + tests) — ADR-0024, 2026-09-21. Everything about `onboard` addresses somebody who is usually not at this machine, and three findings from the perp agent's sessions applied here unchanged. **(1) `--open` was opt-in and nobody passed it.** The page now opens by itself, with the link still printed first and four named stoppers, each said in one line: `--no-open` for a run; `WATERX_PREDICT_NO_BROWSER` or a host `resolveOpener` already refuses (CI, no display, no opener, unsupported platform); this exact link was opened here recently (`onboard --wait` is re-run constantly, and opening every time turns a five-minute wait into twenty tabs — the memory is 0600 beside the other state, and an unreadable one counts as none); or the opener failed, which is reported and stepped over. `--open` overrides the memory. **(2) `--qr`.** The encoder is PORTED from the perp agent with its reference fixtures (generated from the npm `qrcode` package) and its tests, rather than rewritten — its header records two bugs that still produced something that looked like a QR code. It draws under the link, never instead of it. **(3) An agent invented a prohibition** (`--no-open`, on its own initiative). `--no-open` is now forbidden by name and `--qr` named in BOTH documents a host reads, held by a test that fails if either stops saying it, and — the part that actually reached a relaying agent — `next`'s owner state names `onboard --qr` in the hand-over and `--no-open` in the step it hands over. The measurement that found the gap (how many times the surfaces an agent reads name an option) is a test now. `install:check` and `cli:bundle:check` set `WATERX_PREDICT_NO_BROWSER`: a check that put a window on a reviewer's desk is one people stop running. |
| 3.25 | The policy choice arrives with the grant | DONE (code + tests) — ADR-0025, 2026-09-21. Trading needs TWO permissions from two people: the owner's on-chain delegation, and the operator's execution policy. The first had the whole hand-over built around it; the second was reachable and easy to miss, and on mainnet it is read-only by default (ADR-0017) — so a just-authorized agent still could not place an order, and two surfaces said otherwise at exactly the wrong moment. `onboard --wait` printed "Authorized. This agent may now trade" the second the grant landed (false under the default policy), and `next` answered READY ending "Ask the user what to trade", with the posture buried mid-sentence and `market.search` as its first suggestion — so `meta.nextCommand` led AWAY from the only thing blocking it. Now: `onboard` says the runtime still places no order when the policy is read-only and points at `next`; `next` puts `runtime.policy` FIRST when writes are refused (so the pointer names it) and leads the headline with what it cannot do, why, and both commands. The chooser still offers three modes and choosing stays the operator's — this decides WHEN they are asked, not what they pick. Walked live on testnet to the authorize page (`next` → `configure --fromKeystore` → `next` → `onboard`, each pointing at the next); the leg after a real signature is covered through the dispatcher in tests, because a grant needs a person's wallet. |
| 3.26 | The three modes are a screen somebody can read | DONE (code + tests) — ADR-0026, 2026-09-21. A real install session (`bf9f1233`) walked the whole chain on mainnet — install, `configure --fromKeystore`, `onboard --wait`, the page opening itself, the owner's signature, `next` — and landed exactly where ADR-0025 sends it: `then: waterx-predict policy`. It then ran `policy --json | head -80` and **built its own table** of the three modes, from a document `head` had cut off mid-way. It did that well and refused to recommend one, but what reached the operator was a model's paraphrase of three descriptions of what may be signed against real money. The chooser existed only as data. Now `policy` prints it: current mode and where it came from, the config file, then the three in rank order with what each allows, what each costs, the prerequisite where there is one, and the exact command — on stderr (stdout stays one JSON document), prose wrapped to `80 − indent` so the screen fits a terminal by construction, commands never wrapped. `renderChooser` is a pure function, so the screen is a tested claim; five tests cover the marking, the widths, the costs, the prerequisite and survival of `| head -40`. SKILL now tells a relaying agent to pass the screen on as printed, not to summarise it into one recommendation, and never to run `policy set` itself. |
| 3.27 | The choice arrives without being fetched | DONE (code + tests) — ADR-0027, 2026-09-22. A session (`0ae74d20`) walked the whole chain on mainnet from a bare machine — `keystore init --no-passphrase`, `configure --fromKeystore --replace`, `onboard --wait`, the page opening itself, the signature — and then needed THREE commands after that signature before a person saw their options (`onboard` said run `next`, `next` said run `policy`, `policy` printed them). The moment a person's attention is on the screen is the moment they finish signing, and at that moment the tool said what to run rather than showing it. `onboard` now prints the chooser (ADR-0026's screen, one rendering) when a wait ends READY and the policy is read-only. **Printed, never applied, and never asked:** no prompt, because an unattended host cannot answer one — the failure ADR-0020 removed — and an attended one should not have it answered FOR the person who owns the decision; a model driving a terminal types `2` as easily as a person does, and `--yes` exists to be a person saying so in a command they chose to run. The same session also met `The agent wallet is 0x3c92…, but the keystore holds 0xd436…`, went looking in `~/.waterx`, never found `~/.config/waterx-predict/config.json`, and concluded the address was "a package-shipped default" — then passed `--replace` on that false premise. The mismatch now names the source (`(from /Users/…/config.json)` or `(from WATERX_PREDICT_AGENT_WALLET)`), carried as `ResolvedConfig.agentWalletSource` the way `policy.source` always has been. |
| 3.28 | The network is stated, and the pointer survives a cut | DONE (code + tests) — ADR-0028, 2026-09-22. **(1) Mainnet stopped reading as an unmade decision.** ADR-0011 made it the default, but every surface phrased it as a pending choice: the every-answer warning ended `Set WATERX_PREDICT_ENVIRONMENT=testnet to practise`, the hand-over listed "the network" among what a person must supply and hedged with "unless they name a network", and the one step for it was an `export`. A model host duly relayed "set WATERX_PREDICT_ENVIRONMENT=testnet before authorizing" — advice it cannot follow, since a variable exported in one child process is gone by the next (ADR-0020). The warning is now a statement (`This runtime trades on production (mainnet, …), where orders spend real funds.`), the hand-over asks for a wallet and a policy and nothing else, and the remaining deployment step — reachable only when a NAMED deployment is unknown to this build — says `unset WATERX_PREDICT_ENVIRONMENT`, because the fix is removing a wrong value rather than choosing a network. `testnet` stays a documented development switch. **No network-choosing command was added**: the chooser pattern exists for the decision that gates money, and a second one for a setting with a correct default would teach an operator that the default is provisional. **(2) `meta` is serialized before `data`.** Key order means nothing in JSON and decides everything about a truncated read: `next --json` is ~143 lines, `meta.nextCommand` sat on line 136, and three real sessions piped it through `head -100`. The envelope's own fields now come first, so `head -20` carries them; two tests pin the ordering for a success and a refusal. |

## 6. Backend dependencies

Ordered by what blocks the most SDK work.

| # | Capability | Status | Blocks |
| --- | --- | --- | --- |
| B1 | Agent-readable effective risk limits | DONE (backend `7ecad3f3`) | Unblocked 1.7. `GET agent-api/v1/predict/accounts/:accountId/effective-limits` on the **agent** controller returns limits, rolling-window usage, allowance, on-chain delegation and blockers. Read-only: ADR-0003 stands, and the owner-authenticated controller keeps every write. `order preview` and `account risk-limits` now report the real mandate instead of a reason; absence is still absence (`NOT_READ` / `NO_RISK_PROFILE`), never an unlimited default. |
| B2 | Market search / aliases for server-side resolution | DONE (backend `7ecad3f3`) | Unblocked natural-language resolution. `ListMarketsQuery.search` matches published `aliases` server-side and `ListMarketsResponseBody.resolution` carries the verdict. Matching is deterministic and purely lexical — every query token must prefix an alias token, no fuzzy distance, no synonym table — so the same text against the same catalog always resolves the same way. Candidate order is a tie-break (specificity, `closesAt`, id), **not** a ranking of which market is worth trading, and it is documented as such at the contract, command and CLI layers. `marketId` is non-null only on a unique match, so ADR-0001 §10 holds: identity is resolved by the server or not at all. |
| B3 | Quote WS: snapshot + monotonic sequence + gap + SLO | DONE (backend `2aedb8d8`) | Unblocked 2.1 and 2.3. Snapshot on every subscribe, `seq` monotonic per (connection, topic), `gap: true` on a resumed snapshot, heartbeat, per-topic rejection, subscription cap and rate limit, and freshness facts on every frame. Two honest limits are part of the capability, not caveats bolted on: there is **no replay** (a state feed writes nothing per tick — the snapshot is the recovery), and it is **not low-latency** (the server polls an upstream cache; the SLO is stated as `value_age_seconds` P95 ≤ 8 s / P99 ≤ 15 s, with `delivery_lag_seconds` P99 ≤ 1 s as the alertable WaterX-controlled portion). Size-aware depth is still B5; a machine-readable tradeability reason is still B4. |
| B4 | Machine-readable tradeability reason code (closed set) | TODO | 2.9 — ADR-0004 §8. `tradeabilityReason` is free text today. |
| B5 | Size-aware executable quote | TODO | Honest large-size preview. Today `availableSize`, `expectedFillSize`, `feeAmount` are null and `qualityFlags` carries `TOP_OF_BOOK_ONLY`. |
| B6 | Cursor pagination for executions / fills / positions | DONE (backend `2e247fc4`) | Unblocked complete history reconstruction. An opaque base64url keyset cursor anchored on a **row id**, not a timestamp — `TIMESTAMPTZ` is microsecond-precision and a JS `Date` is millisecond, so a timestamp cursor loses rows at the boundary. Keyset, not offset: rows arrive at the head mid-walk, and an offset would repeat or skip. `limit` alone still works, so the change is additive. Every cursor is version-tagged, kind-tagged and canonically re-encoded on decode, and is refused as `INVALID_REQUEST` if it is malformed, truncated, non-canonically spelled, minted for a different list, or names a row belonging to another agent — silently ignoring one restarts the page at the newest row and the caller double-counts it. `nextCursor: null` is proven by reading `limit + 1`, never inferred from page length. **`market list` deliberately gets no cursor**: the catalog is a mutable set with no stable insert order to anchor on, so `?cursor=` there is a 400 rather than a no-op, and the CLI rejects the flag before any request. |
| B7 | Server capability advertisement | TODO | 3.3 |
| B9 | A read from an idempotency key to an execution | TODO | The one `UNKNOWN_PENDING` case the Runner cannot resolve (2.6). A crash *during* `POST /executions` leaves a durable idempotency key and no execution id, and nothing in this API says whether that key produced an execution: no endpoint accepts the key, `PredictExecutionSummary` does not carry it, and `ListExecutionsQuery` cannot filter on it. Replaying the create as a probe is not a workaround — a key that never landed would be *made* to land by the probe, reserving allowance and creating the order the caller was trying to find out about, and the exact request bytes died with the process in any case. `reconcileJob` therefore reports `INCONCLUSIVE` and leaves the job visible in `UNKNOWN_PENDING`. Smallest sufficient fix: echo `idempotencyKey` on `PredictExecutionSummary` **and** accept it as a `ListExecutionsQuery` filter, so the answer is one authoritative read rather than a scan. |
| B8 | Performance reads: realized PnL, win rate, trade count, attribution | DONE (backend `5ab9d9e4`) | Unblocked Scenario 5 and `account performance`. `GET agent-api/v1/predict/accounts/:accountId/performance` returns order outcomes, rejection reasons by (code, stage), realized PnL and the exclusions, optionally narrowed by `strategyId`. **D-24 is answered the conservative way and the answer is structural, not documentary**: `attributionScope` is `API_ATTRIBUTED_ONLY`, it is the only value the endpoint can produce, and direct-chain activity by the same delegated key is absent at any confidence rather than blended in — inventing an entry price for a trade this backend never priced would yield a win rate that looks complete while being wrong. Cost basis is the exact amount each close released from deployed budget (the `CLOSE_RELEASE` ledger row, i.e. the contract's own `split_cost`), so realized PnL reconciles against the allowance projection by construction rather than by a proportion inferred at read time. Rates divide by the population that makes them honest — `successRate` by **terminal**, not created — and every rate is `null` when its denominator is zero, because `"0"` there reads as "everything lost". What is excluded is counted, not prosed: `exitsWithoutAttributedBasis`, `claimedPositions` (the population that biases `winRate` **downward**, and by the most, since a resolved market is claimed from the FE) and `openPositions`. There is **no time window**, because the claim exclusions carry no instant to window on and a windowed total would silently disagree with them. **Not verified here:** the two aggregate SQL specs (`predict-agent-execution.repository.e2e.spec.ts`, `predict-agent-position-attribution.repository.e2e.spec.ts`) need a real Postgres and have not been executed in this environment. |

## 7. Open questions not yet ADR'd

D-23 and D-25 remain deliberately undecided. Neither gates Phase 0 or Phase 1.
D-24 was answered by B8; D-26 through D-30 are decided by ADR-0009 and are kept
here with their answers rather than deleted, so a reader who remembers the
question finds the ruling instead of a gap:

| ID | Question | Gates |
| --- | --- | --- |
| D-23 | Notification channels | 2.13 |
| D-24 | Whether agent performance includes direct-chain trades | ~~B8~~ — answered by B8: it does not, and `attributionScope: API_ATTRIBUTED_ONLY` is the only mode the endpoint can produce. Revisiting it would need a chain-side attribution source, not a flag. |
| D-25 | Historical quote window and granularity | Scenario 1 |
| D-26 | Runtime auto-update with active jobs | ~~3.6~~ — decided by ADR-0009: **there is no auto-update, on any channel, in any package**. An update is an operator action, and a Runner with active jobs is drained first. The drain that gates admission is 2.14 and now exists: `runner.drain` refuses admission, settles this instance's in-flight work, and reports an exceeded deadline instead of crossing it. |
| D-27 | Release artifacts (provenance, SBOM, container) | ~~3.6~~ — decided by ADR-0009: provenance and a committed CycloneDX SBOM are **mandatory** for every published package; **no container image**, because nothing here is a service and an image would be a second, staler way to install a library. |
| D-28 | Whether MCP ships in the first wave | ~~3.6~~ — decided by ADR-0009: **no**. The first wave is the SDK and the schema. The MCP server, the adapters, the CLI and the Runner are built and stay `private` until 1.11 has run against a real server and 2.14 exists — an adapter that spawns an unpublished CLI would install into a host that cannot run it. |
| D-29 | CLI/local API version support window | ~~3.6~~ — decided by ADR-0009: **refuse across a version boundary rather than degrade**; reads may proceed, writes are refused. Supported window is the current minor plus the one before it, with the published packages versioned in lockstep. |
| D-30 | Telemetry and privacy defaults | ~~3.6~~ — decided by ADR-0009: **none**. No metrics endpoint, no crash reporter, no usage ping, no update check, in any package. Adding any requires a superseding ADR, and it would be opt-in and off by default. |

## 8. Release-readiness audit — 2026-08-18

An audit of the plan against the paths a consumer can actually reach. Four
findings; two are fixed in this commit, two are recorded because they cannot be
fixed here.

| Finding | Disposition |
| --- | --- |
| **Fourteen contract constants were unreachable from an installed package.** `export type *` re-exports types and no runtime values, and the `exports` map blocks a deep import, so the routes, the idempotency header and the stream caps existed in the tarball and could not be imported from it. | **Fixed.** `packages/sdk/src/index.ts` exports them by value; `packages/sdk/tests/entry-point.test.ts` imports through the entry point so the test fails the way a consumer would. |
| **This file's own verification header had been stale for several commits** — 1012 tests in 50 files against a measured 1080 in 55, and a backend pin two commits behind. A status file that reports the wrong number is worse than one that reports none, because the wrong number is quoted. | **Fixed.** Re-measured and re-pinned above, and the counts are now stated per package so the next drift is visible. |
| **`xmlhttprequest-ssl@2.1.2` declares no `license` field.** It reaches a consumer transitively through `socket.io-client` → `engine.io-client`. A generator that guessed MIT here would be right and would still be guessing. | **Recorded, not guessed.** Read by a human, pinned with its evidence in `packages/release/src/license-review.ts`, emitted as `HUMAN_REVIEW`, and failed by the preflight if the version it covers stops shipping. |
| **No repository URL is configured**, so npm provenance has nothing to attest the build to. | **Unresolved, and blocking.** The preflight reports it as UNRESOLVED and `--strict` refuses on it, so the release workflow stops before a version is burned. Nobody in this repository can supply a remote. |

What the audit did **not** establish, stated so it is not read into the above:
no package has been published; no provenance attestation has ever been produced;
neither workflow in `.github/workflows/` has ever run on a real runner
(`actionlint` is clean, which is a syntax fact); the install matrix covered
macOS/arm64 on Node 20, 22 and 26 only; and 1.11 is still the unmoved item —
nothing here has been driven against a live server.

## 9. Standing rules for updating this file

- Move an item to **DONE** only when the public path *and* its failure/recovery
  behavior are implemented and tested. Documentation or scaffolding is not DONE.
- A capability may be advertised by `describe` only when it is DONE here.
- Re-diff the vendored contract against the backend whenever a wire surface
  changes, and update the "Contract sync" section with the commits compared.
- A reserved package boundary is never evidence for the item that names it.
  `packages/mcp` existing did not advance 3.2 — a projection of the contract, a
  transport that serves it and a test that runs one intent through both front
  doors did — and a Runner that starts, holds
  leases and answers a socket does not make 2.6 DONE — a job that nothing drives
  is not a running strategy, however reachable the process holding it is. A
  `JobScheduler` that exists does not change that either: the loop is DONE, but
  2.6 stayed PARTIAL until the process an operator actually starts could build a
  driver, because a loop with no price source and no signer drives nothing. The
  test set for that was `runnerd` printing `driving: true` rather than the package
  being capable of it, and a configured `runnerd` now does. The bar moved again
  rather than disappearing: a strategy can now be handed to that process over the
  socket, and as of this commit an operator can hand it one with a **command** —
  so 2.8 is DONE by this file's own definition, its public path and its three
  money-losing failure modes implemented and tested. Neither this rule nor that
  one is satisfied by a library an application could call, and a socket protocol
  with no client was one step short of the same thing; that step is taken. What
  is deliberately **not** claimed by it: nobody has watched a strategy fill
  against a live server. That is 1.11, it is the harder half, and moving 2.8 does
  not move it — 2.6 stays PARTIAL for the daemon lifecycle nothing here manages,
  and the Phase 2 exit criteria are still unobserved.
- A command in `schemas/v1/agent-commands.json` describes an intent; it is not
  evidence that a surface can execute it. Only add one when the execution core
  can perform it.
- When an item moves, update the evidence column with a real path or test name.
  An item with no evidence is not DONE.
- Removing `private: true` from a package is a release decision, not a build one.
  It requires the ADR-0009 conditions to hold, and it changes what `sbom/v1/`
  must contain — `pnpm sbom:check` fails on a published package with no SBOM and
  on an SBOM belonging to no published package.
- Re-measure the verification header rather than editing the sentence around it.
  Every number in it is output from a command that was run in the commit that
  states it.
