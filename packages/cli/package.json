{
  "name": "@waterx/predict-agent-cli",
  "version": "0.0.0",
  "private": true,
  "description": "RESERVED BOUNDARY, NOT IMPLEMENTED. The `waterx-predict` CLI: the canonical universal-agent surface over the command contract.",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=20" }
}
