{
  "name": "@waterx/predict-agent-signer-keystore",
  "version": "0.1.0",
  "private": true,
  "description": "A SIGNER_PROTOCOL v1 provider for unattended signing: an ssh-agent-shaped keystore. The passphrase is given once, a resident agent holds the decrypted key in memory, and a thin per-request client reaches it over a private socket. The only provider a Runner can use.",
  "license": "MIT",
  "type": "module",
  "bin": { "waterx-predict-keystore": "dist/src/bin/keystore.js" },
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.build.json && node -e \"require('node:fs').chmodSync('dist/src/bin/keystore.js',0o755)\"",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@mysten/sui": "^2.9.1"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.6"
  }
}
