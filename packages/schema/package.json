{
  "name": "@waterx/predict-agent-schema",
  "version": "0.1.0",
  "description": "Versioned, runtime-validated command contract for the WaterX Predict agent runtime — one source of truth for the CLI, the emitted JSON Schema and every adapter.",
  "license": "MIT",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "files": ["dist", "agent-commands.json", "LICENSE", "package.json", "README.md"],
  "engines": { "node": ">=20" },
  "publishConfig": { "access": "public", "provenance": true },
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "generate": "pnpm run build && node dist/src/generate.js ../../schemas/v1/agent-commands.json agent-commands.json",
    "generate:check": "pnpm run build && node dist/src/generate.js ../../schemas/v1/agent-commands.json agent-commands.json --check"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.6"
  }
}
